---
phase: 01-intelig-ncia-conversacional
plan: 02
type: execute
wave: 2
depends_on: ["01-01-PLAN.md"]
files_modified:
  - app/router.py
  - app/agents/orchestrator.py
  - tests/test_router.py
autonomous: true
requirements:
  - INTL-01
  - INTL-04
must_haves:
  truths:
    - "Estado de conversa e carregado do Redis no inicio de cada mensagem e salvo ao final"
    - "Orquestrador classifica intencao em TODA mensagem, mesmo com agente ativo"
    - "Intencoes remarcar, cancelar, duvida_clinica interrompem o fluxo atual e trocam de agente"
    - "Intencoes tirar_duvida, fora_de_contexto sao respondidas inline sem sair do fluxo"
    - "Paciente de retorno e cumprimentado pelo nome (Contact.first_name)"
    - "Falha do Redis nao trava o sistema — cria estado novo com mensagem amigavel"
  artifacts:
    - path: "app/router.py"
      provides: "Roteamento com Redis state + context-aware orchestrator + interrupt detection"
      contains: "RedisStateManager"
    - path: "app/agents/orchestrator.py"
      provides: "Funcao rotear() com parametro agente_ativo para classificacao sempre"
      contains: "agente_ativo"
    - path: "tests/test_router.py"
      provides: "Testes de interrupt detection, Redis integration, reconhecimento por nome"
      contains: "test_interrupt"
  key_links:
    - from: "app/router.py"
      to: "app/state_manager.py"
      via: "state_mgr.load/save/delete chamado em route_message"
      pattern: "state_mgr\\.(load|save|delete)"
    - from: "app/router.py"
      to: "app/agents/orchestrator.py"
      via: "rotear() chamado SEMPRE, mesmo com agente ativo"
      pattern: "rotear\\("
    - from: "app/router.py"
      to: "app/models.py Contact.first_name"
      via: "Carrega first_name do banco para saudacao personalizada"
      pattern: "contact\\.first_name"
---

<objective>
Substituir o dict in-memory `_AGENT_STATE` pelo `RedisStateManager` e tornar o orquestrador context-aware: classifica intencao em TODA mensagem (mesmo com agente ativo), detecta interrupcoes (remarcar, cancelar, duvida_clinica) e trata intencoes inline (tirar_duvida, fora_de_contexto) sem resetar o fluxo. Tambem implementa reconhecimento de paciente por nome (D-14).

Purpose: Sem isso o estado se perde a cada reinicio e a Ana nao consegue adaptar o fluxo quando o paciente muda de assunto — pilares centrais da INTL-01.

Output: `app/router.py` refatorado com Redis + interrupt detection, `app/agents/orchestrator.py` expandido, testes atualizados.
</objective>

<execution_context>
@C:\Users\Breno\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Breno\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intelig-ncia-conversacional/01-CONTEXT.md
@.planning/phases/01-intelig-ncia-conversacional/01-01-SUMMARY.md

<interfaces>
<!-- Contratos criados pelo Plan 01-01 que este plan consome -->

From app/state_manager.py:
```python
class RedisStateManager:
    KEY_PREFIX = "agent_state:"
    def __init__(self, redis_url: str) -> None: ...
    async def load(self, phone_hash: str) -> object | None: ...
    async def save(self, phone_hash: str, agent: object) -> None: ...
    async def delete(self, phone_hash: str) -> None: ...
```

From app/agents/atendimento.py:
```python
class AgenteAtendimento:
    def __init__(self, telefone: str, phone_hash: str): ...
    def to_dict(self) -> dict: ...  # _tipo: "atendimento"
    @classmethod
    def from_dict(cls, data: dict) -> "AgenteAtendimento": ...
    def processar(self, msg: str) -> list[str]: ...
    etapa: str  # FSM stage
    nome: str | None
    historico: list[dict]
```

From app/agents/retencao.py:
```python
class AgenteRetencao:
    def __init__(self, telefone: str, nome: str | None = None, modalidade: str = "presencial"): ...
    def to_dict(self) -> dict: ...  # _tipo: "retencao"
    @classmethod
    def from_dict(cls, data: dict) -> "AgenteRetencao": ...
    def processar_remarcacao(self, msg: str) -> list[str]: ...
    def processar_cancelamento(self, msg: str) -> list[str]: ...
    etapa: str
    historico: list[dict]
```

From app/agents/orchestrator.py:
```python
IntencaoType = Literal["novo_lead", "tirar_duvida", "agendar", "pagar",
                       "remarcar", "cancelar", "fora_de_contexto", "duvida_clinica"]
_INTENCOES_AGENTE1: set[str] = {"novo_lead", "tirar_duvida", "agendar", "pagar"}
_INTENCOES_AGENTE2: set[str] = {"remarcar", "cancelar"}
def rotear(mensagem: str, stage_atual: str | None, primeiro_contato: bool = False) -> dict: ...
```

From app/models.py:
```python
class Contact(Base):
    phone_hash: Mapped[str]
    phone_e164: Mapped[str | None]
    push_name: Mapped[str | None]
    stage: Mapped[str]
    collected_name: Mapped[str | None]
    first_name: Mapped[str | None]
    last_name: Mapped[str | None]
    dietbox_patient_id: Mapped[int | None]
```

From app/meta_api.py:
```python
class MetaAPIClient:
    async def send_text(self, to: str, text: str) -> dict: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refatorar router.py — Redis state + orquestrador sempre ativo + interrupt detection</name>
  <files>app/router.py, app/agents/orchestrator.py, tests/test_router.py</files>
  <read_first>app/router.py, app/agents/orchestrator.py, app/state_manager.py, app/models.py, app/main.py, tests/test_router.py</read_first>
  <behavior>
    - Test 1: route_message carrega estado do Redis via state_mgr.load() no inicio
    - Test 2: route_message salva estado via state_mgr.save() apos agente processar
    - Test 3: route_message deleta estado via state_mgr.delete() quando etapa == "finalizacao" ou "concluido"
    - Test 4: com agente ativo (AgenteAtendimento em etapa "escolha_plano"), mensagem "quero remarcar" classifica intencao e troca para AgenteRetencao (interrupt detection D-02)
    - Test 5: com agente ativo (AgenteAtendimento em etapa "escolha_plano"), mensagem "quanto custa?" (tirar_duvida) responde inline e mantem AgenteAtendimento na mesma etapa (D-03)
    - Test 6: com agente ativo, mensagem "fora_de_contexto" responde inline e mantem agente atual (D-03)
    - Test 7: paciente com Contact.first_name="Marcela" recebe saudacao personalizada com nome (D-14)
    - Test 8: falha do Redis (load retorna None) cria estado novo sem crash (D-15)
    - Test 9: rotear() agora recebe parametro agente_ativo (tipo string ou None) para contexto
    - Test 10: _AGENT_STATE dict in-memory nao existe mais em router.py
  </behavior>
  <action>
**1. Expandir `app/agents/orchestrator.py` — rotear() com contexto de agente ativo:**

Adicionar parametro `agente_ativo: str | None = None` a `rotear()`. Adicionar no prompt de classificacao o contexto do agente ativo para que o LLM saiba que ha um fluxo em andamento:

```python
def rotear(
    mensagem: str,
    stage_atual: str | None,
    primeiro_contato: bool = False,
    agente_ativo: str | None = None,  # "atendimento" | "retencao" | None
) -> dict:
```

No prompt, se `agente_ativo` nao for None, adicionar contexto:
```
Contexto: o paciente esta no meio de um fluxo de {agente_ativo}. Classifique a intencao real da mensagem mesmo assim.
```

Isso garante D-01: orquestrador classifica sempre.

**2. Refatorar `app/router.py` completamente:**

Remover `_AGENT_STATE` dict, `_get_or_create_atendimento`, `_get_or_create_retencao`.

Inicializar `RedisStateManager` globalmente (import lazy no topo do modulo ou via parametro). Usar variavel global `_state_mgr: RedisStateManager | None = None` inicializada em `init_state_manager(redis_url)` chamado no lifespan do app.

Adicionar funcao `init_state_manager(redis_url: str)`:
```python
_state_mgr: RedisStateManager | None = None

def init_state_manager(redis_url: str) -> None:
    global _state_mgr
    from app.state_manager import RedisStateManager
    _state_mgr = RedisStateManager(redis_url)
```

Refatorar `route_message()`:

```python
async def route_message(phone: str, phone_hash: str, text: str, meta_message_id: str):
    # 1. Setup meta client (igual ao atual)
    # 2. Carregar contato do banco
    # 3. Carregar estado do Redis
    agente_ativo = await _state_mgr.load(phone_hash) if _state_mgr else None

    # 4. Determinar tipo do agente ativo
    tipo_agente = None
    if isinstance(agente_ativo, AgenteAtendimento):
        tipo_agente = "atendimento"
    elif isinstance(agente_ativo, AgenteRetencao):
        tipo_agente = "retencao"

    # 5. SEMPRE classificar intencao (D-01)
    rota = rotear(
        mensagem=text,
        stage_atual=stage,
        primeiro_contato=primeiro_contato,
        agente_ativo=tipo_agente,
    )
    intencao = rota["intencao"]
    agente_destino = rota["agente"]

    # 6. Reconhecimento por nome (D-14)
    nome_saudacao = contact.first_name or nome
    if agente_ativo is None and nome_saudacao and not primeiro_contato:
        # Paciente de retorno reconhecido
        saudacao = f"Eiii {nome_saudacao}, lembro que ja nos falamos! Como posso te ajudar? 💚"
        # Enviar saudacao apenas se nao ha agente ativo (primeira msg da sessao)

    # 7. Interrupt detection (D-02, D-03)
    if agente_ativo is not None:
        _INTENCOES_INTERRUPT = {"remarcar", "cancelar", "duvida_clinica"}
        _INTENCOES_INLINE = {"tirar_duvida", "fora_de_contexto"}

        if intencao in _INTENCOES_INTERRUPT:
            # Salvar nome do agente antigo no Contact se disponivel
            if hasattr(agente_ativo, "nome") and agente_ativo.nome:
                _salvar_nome_contact(phone_hash, agente_ativo.nome)
            # Deletar estado antigo, vai cair no roteamento normal abaixo
            if _state_mgr:
                await _state_mgr.delete(phone_hash)
            agente_ativo = None
            # Nao fazer return — cai no bloco de roteamento abaixo

        elif intencao in _INTENCOES_INLINE:
            # Responder inline sem sair do fluxo (D-03)
            resposta_inline = rota.get("resposta_padrao") or "Posso te ajudar com agendamentos e informacoes sobre as consultas 💚"
            await _enviar(meta, phone, [resposta_inline])
            # Salvar estado (agente NAO mudou)
            if _state_mgr:
                await _state_mgr.save(phone_hash, agente_ativo)
            return

        else:
            # Intencao compativel com agente atual — continuar no fluxo
            if isinstance(agente_ativo, AgenteAtendimento):
                respostas = agente_ativo.processar(text)
            elif isinstance(agente_ativo, AgenteRetencao):
                if agente_ativo.etapa in ("aguardando_motivo", "coletando_nome_cancel"):
                    respostas = agente_ativo.processar_cancelamento(text)
                else:
                    respostas = agente_ativo.processar_remarcacao(text)
            await _enviar(meta, phone, respostas)
            # Salvar estado atualizado + limpar se finalizado
            if _state_mgr:
                if _fluxo_finalizado(agente_ativo):
                    await _state_mgr.delete(phone_hash)
                else:
                    await _state_mgr.save(phone_hash, agente_ativo)
            return

    # 8. Roteamento para agente (mesmo codigo de antes, mas salvar no Redis)
    # ... criar agente, processar, salvar via _state_mgr.save()
    # ... ao final: if _fluxo_finalizado(agent): await _state_mgr.delete(phone_hash)
```

Adicionar helper `_fluxo_finalizado(agent) -> bool`:
```python
def _fluxo_finalizado(agent) -> bool:
    if isinstance(agent, AgenteAtendimento):
        return agent.etapa == "finalizacao"
    if isinstance(agent, AgenteRetencao):
        return agent.etapa == "concluido"
    return False
```

Adicionar helper `_salvar_nome_contact(phone_hash, nome)` que faz UPDATE no Contact.collected_name e Contact.first_name.

**3. Atualizar `app/main.py` lifespan** para chamar `init_state_manager()`:

No bloco `@asynccontextmanager async def lifespan(app)`, adicionar:
```python
from app.router import init_state_manager
redis_url = os.environ.get("REDIS_URL", "redis://redis:6379")
init_state_manager(redis_url)
```

**4. Atualizar `tests/test_router.py`:**

Reescrever os testes existentes para usar mocks do RedisStateManager em vez de `_AGENT_STATE`.

Adicionar novos testes para:
- Interrupt detection (D-02): agente ativo + intencao "remarcar" → troca de agente
- Inline response (D-03): agente ativo + intencao "tirar_duvida" → responde sem trocar
- Reconhecimento por nome (D-14): Contact.first_name presente → saudacao personalizada
- Redis failure graceful (D-15): load retorna None → fluxo normal sem crash
- Estado salvo no Redis apos processamento
- Estado deletado apos finalizacao

Usar `unittest.mock.AsyncMock` para mockar `_state_mgr.load/save/delete` e `meta.send_text`.
Usar `patch("app.agents.orchestrator._classificar_intencao")` para controlar intencao.

**IMPORTANTE:**
- Remover completamente `_AGENT_STATE` e as funcoes `_get_or_create_atendimento` e `_get_or_create_retencao`
- Manter `_enviar()`, `_resumir_historico()`, `_inferir_modalidade()` como helpers
- Adaptar `_inferir_modalidade()` para receber agente como parametro em vez de buscar no dict
- Numero 31 99205-9211 NAO aparece neste arquivo (D-08, INTL-04)
  </action>
  <verify>
    <automated>python -m pytest tests/test_router.py tests/test_state_manager.py -x -v</automated>
  </verify>
  <done>
    - `_AGENT_STATE` dict removido de router.py
    - RedisStateManager integrado: load no inicio, save apos processar, delete em finalizacao
    - Orquestrador chamado SEMPRE (D-01): rotear() recebe agente_ativo como contexto
    - Interrupt detection funciona (D-02): remarcar/cancelar/duvida_clinica troca de agente
    - Inline response funciona (D-03): tirar_duvida/fora_de_contexto responde sem trocar
    - Paciente de retorno cumprimentado por nome (D-14)
    - Falha do Redis nao trava (D-15)
    - Todos os testes passam (novos + existentes adaptados)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis data store | Estado de conversa serializado; historico limitado a 20 entradas |
| LLM classification | Orquestrador classifica intencao via Claude Haiku — resultado determina roteamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | app/router.py | mitigate | Validar intencao retornada pelo orquestrador contra set de intencoes validas antes de rotear |
| T-01-05 | Information Disclosure | app/router.py | mitigate | Numero interno 31 99205-9211 nao presente no arquivo; phone_hash usado como chave Redis (nunca numero real) |
| T-01-06 | Denial of Service | app/router.py Redis | mitigate | Redis failure retorna None via try/except em state_manager.py; route_message continua com estado novo (D-15) |
| T-01-07 | Elevation of Privilege | app/agents/orchestrator.py | accept | Paciente pode manipular classificacao com prompt injection no texto; risco baixo pois agentes so fazem agendamento — nenhuma acao destrutiva |
</threat_model>

<verification>
1. `python -m pytest tests/test_router.py -x -v` — testes de interrupt detection, Redis, reconhecimento passam
2. `python -m pytest tests/test_state_manager.py -x -v` — testes de serializacao continuam passando
3. `grep -r "_AGENT_STATE" app/router.py` — nao deve retornar nada (dict removido)
4. `grep -r "99205" app/router.py` — nao deve retornar nada (numero interno nao exposto)
5. `python -m pytest tests/ -q` — suite completa sem regressoes
</verification>

<success_criteria>
- `_AGENT_STATE` dict nao existe mais em router.py
- RedisStateManager usado para load/save/delete em route_message
- rotear() aceita parametro agente_ativo e classifica TODA mensagem
- Interrupcao: remarcar/cancelar/duvida_clinica troca agente sem perder nome
- Inline: tirar_duvida/fora_de_contexto responde sem sair do fluxo
- Paciente reconhecido por first_name recebe saudacao personalizada
- Falha Redis nao causa crash
- Todos os testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/01-intelig-ncia-conversacional/01-02-SUMMARY.md`
</output>
