---
phase: 01-intelig-ncia-conversacional
plan: 03
type: execute
wave: 3
depends_on: ["01-02-PLAN.md"]
files_modified:
  - app/escalation.py
  - app/webhook.py
  - app/meta_api.py
  - app/knowledge_base.py
  - app/agents/atendimento.py
  - app/remarketing.py
  - tests/test_escalation.py
  - tests/test_behavior.py
autonomous: true
requirements:
  - INTL-02
  - INTL-03
  - INTL-04
  - INTL-05
must_haves:
  truths:
    - "Duvida clinica de paciente cadastrado resulta em envio do contato da Thaynara (5531991394759) sem escalar internamente"
    - "Duvida clinica de lead ou pergunta que Ana nao sabe resulta em relay para Breno com contexto — paciente recebe 'So um instante, vou verificar essa informacao'"
    - "Numero 31 99205-9211 NUNCA aparece em mensagem ao paciente"
    - "Breno recebe lembrete a cada 15min na primeira hora, depois a cada 1h"
    - "Apos 1h sem resposta do Breno, paciente recebe aviso de que Ana ainda esta verificando"
    - "Resposta do Breno e salva na knowledge base como FAQ aprendido"
    - "Ana envia 'Um instante, por favor' ANTES de chamar Dietbox ou gerar link de pagamento"
    - "Tom e mensagens fixas da Ana seguem documentacao oficial"
  artifacts:
    - path: "app/escalation.py"
      provides: "3 caminhos de escalacao + relay bidirecional + schedule de lembretes"
      contains: "criar_escalacao_relay"
    - path: "app/webhook.py"
      provides: "Handler que detecta resposta do Breno e repassa ao paciente"
      contains: "_NUMERO_INTERNO"
    - path: "app/meta_api.py"
      provides: "send_contact() para enviar VCard da Thaynara"
      contains: "send_contact"
    - path: "app/knowledge_base.py"
      provides: "salvar_faq_aprendido() para persistir respostas do Breno"
      contains: "salvar_faq_aprendido"
    - path: "app/agents/atendimento.py"
      provides: "Waiting indicator antes de operacoes demoradas + MSG_* alinhadas com documentacao"
      contains: "Um instante"
    - path: "tests/test_escalation.py"
      provides: "Testes dos 3 caminhos de escalacao + lembretes + FAQ aprendido"
  key_links:
    - from: "app/escalation.py"
      to: "app/models.py PendingEscalation"
      via: "Cria registro na tabela para tracking do relay"
      pattern: "PendingEscalation"
    - from: "app/webhook.py"
      to: "app/escalation.py"
      via: "Detecta mensagem do Breno e chama processar_resposta_breno()"
      pattern: "processar_resposta_breno"
    - from: "app/escalation.py"
      to: "app/knowledge_base.py"
      via: "Salva resposta como FAQ aprendido"
      pattern: "salvar_faq_aprendido"
    - from: "app/agents/atendimento.py"
      to: "app/meta_api.py"
      via: "Envia waiting indicator antes de operacao demorada"
      pattern: "Um instante"
---

<objective>
Implementar os 3 caminhos de escalacao (D-05, D-06, D-07), relay bidirecional com Breno incluindo lembretes e timeout (D-09, D-10), FAQ aprendido (D-11), waiting indicator antes de operacoes demoradas (D-21), e alinhamento de tom/mensagens com documentacao oficial (D-18, D-19).

Purpose: Sem escalacao, a Ana trava quando nao sabe responder. Sem waiting indicator, o paciente acha que a Ana travou durante consultas ao Dietbox. Sem alinhamento de tom, a experiencia nao parece profissional.

Output: Escalacao funcional com 3 caminhos, relay bidirecional, lembretes ao Breno, FAQ aprendido, waiting indicator, mensagens alinhadas.
</objective>

<execution_context>
@C:\Users\Breno\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Breno\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-intelig-ncia-conversacional/01-CONTEXT.md
@.planning/phases/01-intelig-ncia-conversacional/01-01-SUMMARY.md
@.planning/phases/01-intelig-ncia-conversacional/01-02-SUMMARY.md

<interfaces>
<!-- Contratos existentes consumidos por este plan -->

From app/models.py PendingEscalation (criado pelo Plan 01-01):
```python
class PendingEscalation(Base):
    __tablename__ = "pending_escalations"
    id: Mapped[str]             # UUID
    phone_hash: Mapped[str]     # hash do paciente
    phone_e164: Mapped[str]     # numero real do paciente (para envio)
    pergunta_original: Mapped[str]  # o que o paciente perguntou
    contexto: Mapped[str]       # historico resumido
    status: Mapped[str]         # "aguardando" | "respondido" | "timeout"
    created_at: Mapped[datetime]
    responded_at: Mapped[datetime | None]
    resposta_breno: Mapped[str | None]
    next_reminder_at: Mapped[datetime]
    reminder_count: Mapped[int]  # 0, 1, 2, ...
```

From app/escalation.py (atual):
```python
_NUMERO_INTERNO = "5531992059211"  # Breno — NUNCA expor ao paciente
BRT = timezone(timedelta(hours=-3))
async def escalar_para_humano(meta_client, telefone_paciente, nome_paciente, historico_resumido, motivo) -> bool
def build_contexto_escalacao(nome_paciente, telefone_paciente, historico_resumido, motivo) -> str
def _em_horario_comercial(agora=None) -> bool
```

From app/meta_api.py:
```python
class MetaAPIClient:
    async def send_text(self, to: str, text: str) -> dict
    async def send_template(self, to: str, template_name: str, ...) -> dict
    async def _post(self, payload: dict) -> dict
```

From app/knowledge_base.py:
```python
class KnowledgeBase:
    faq_estatico: list[dict[str, str]]
    def faq_combinado(self) -> list[dict[str, str]]
    # _KB_DIR = Path(__file__).parent.parent / "knowledge_base"
```

From app/agents/atendimento.py (metodos que chamam Dietbox/Rede — precisam de waiting indicator):
```python
class AgenteAtendimento:
    def _etapa_agendamento(self, msg) -> list[str]     # chama dietbox_worker.consultar_slots_disponiveis
    def _etapa_cadastro_dietbox(self, msg) -> list[str] # chama dietbox_worker.processar_agendamento
    def _etapa_forma_pagamento(self, msg) -> list[str]  # chama rede_worker.gerar_link_pagamento
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escalacao 3 caminhos + relay bidirecional + lembretes ao Breno</name>
  <files>app/escalation.py, app/webhook.py, app/meta_api.py, app/remarketing.py, tests/test_escalation.py</files>
  <read_first>app/escalation.py, app/webhook.py, app/meta_api.py, app/models.py, app/remarketing.py, app/database.py, app/main.py</read_first>
  <behavior>
    - Test 1: duvida_clinica + paciente cadastrado (dietbox_patient_id not None) → envia VCard Thaynara ao paciente, NAO escala internamente (D-05)
    - Test 2: duvida_clinica + lead (dietbox_patient_id is None) → cria PendingEscalation, envia contexto ao Breno, paciente recebe "So um instante, vou verificar" (D-06)
    - Test 3: Ana nao sabe responder → mesmo fluxo do Test 2: cria PendingEscalation + envia ao Breno (D-07)
    - Test 4: resposta do Breno e detectada pelo webhook → PendingEscalation.status atualizado para "respondido", resposta repassada ao paciente (relay)
    - Test 5: schedule_reminders calcula proximos lembretes: 15min, 30min, 45min, 60min, 2h, 3h... (D-09)
    - Test 6: apos 1h sem resposta, paciente recebe mensagem de aviso (D-10)
    - Test 7: numero 31 99205-9211 NAO aparece em nenhuma mensagem enviada ao paciente (D-08, INTL-04)
    - Test 8: resposta do Breno salva como FAQ aprendido na knowledge base (D-11)
  </behavior>
  <action>
**1. Adicionar `send_contact()` ao `app/meta_api.py`:**

```python
async def send_contact(self, to: str, nome: str, telefone: str) -> dict:
    """Envia VCard de contato via WhatsApp."""
    # Separar primeiro nome e sobrenome
    partes = nome.split(" ", 1)
    first_name = partes[0]
    last_name = partes[1] if len(partes) > 1 else ""
    payload = {
        "messaging_product": "whatsapp",
        "to": to,
        "type": "contacts",
        "contacts": [{
            "name": {
                "formatted_name": nome,
                "first_name": first_name,
                "last_name": last_name,
            },
            "phones": [{
                "phone": telefone,
                "type": "CELL",
            }],
        }],
    }
    return await self._post(payload)
```

**2. Refatorar `app/escalation.py` com 3 caminhos:**

Manter `_NUMERO_INTERNO`, `BRT`, `_em_horario_comercial`, `build_contexto_escalacao`.

Adicionar constantes:
```python
_NUMERO_THAYNARA = "5531991394759"

_MSG_DUVIDA_CLINICA_PACIENTE = (
    "Aqui e um canal somente para marcacao de consultas. "
    "Para duvidas clinicas por favor chame a Thaynara no WhatsApp 💚"
)

_MSG_AGUARDANDO_VERIFICACAO = "So um instante, vou verificar essa informacao 💚"

_MSG_TIMEOUT_1H = "Ainda estou verificando, te aviso assim que tiver retorno 💚"
```

Adicionar funcao principal com 3 caminhos:
```python
async def escalar_duvida(
    meta_client,
    telefone_paciente: str,
    phone_hash: str,
    nome_paciente: str | None,
    historico_resumido: str,
    motivo: str,
    is_paciente_cadastrado: bool,
) -> str:
    """
    Roteia escalacao para o caminho correto.

    Returns: "contato_thaynara" | "relay_breno"
    """
    if motivo == "duvida_clinica" and is_paciente_cadastrado:
        # D-05: envia contato Thaynara
        await meta_client.send_text(telefone_paciente, _MSG_DUVIDA_CLINICA_PACIENTE)
        await meta_client.send_contact(telefone_paciente, "Thaynara Teixeira", _NUMERO_THAYNARA)
        return "contato_thaynara"

    # D-06 e D-07: relay para Breno
    await meta_client.send_text(telefone_paciente, _MSG_AGUARDANDO_VERIFICACAO)
    escalation_id = await criar_escalacao_relay(
        meta_client, phone_hash, telefone_paciente,
        nome_paciente, historico_resumido, motivo,
    )
    return "relay_breno"
```

Adicionar `criar_escalacao_relay()`:
```python
async def criar_escalacao_relay(
    meta_client, phone_hash, telefone_paciente,
    nome_paciente, historico_resumido, motivo,
) -> str:
    """Cria PendingEscalation + envia contexto ao Breno."""
    from app.database import SessionLocal
    from app.models import PendingEscalation

    contexto = build_contexto_escalacao(nome_paciente, telefone_paciente, historico_resumido, motivo)
    now = datetime.now(BRT)
    next_reminder = now + timedelta(minutes=15)

    with SessionLocal() as db:
        esc = PendingEscalation(
            phone_hash=phone_hash,
            phone_e164=telefone_paciente,
            pergunta_original=motivo,
            contexto=contexto,
            status="aguardando",
            next_reminder_at=next_reminder,
            reminder_count=0,
        )
        db.add(esc)
        db.commit()
        esc_id = esc.id

    # Enviar ao Breno
    await meta_client.send_text(_NUMERO_INTERNO, contexto)
    logger.info("Escalacao relay criada id=%s para paciente=%s", esc_id, telefone_paciente[-4:])
    return esc_id
```

Adicionar `processar_resposta_breno()`:
```python
async def processar_resposta_breno(
    meta_client,
    texto_resposta: str,
) -> bool:
    """
    Processa resposta do Breno: encontra PendingEscalation mais recente aguardando,
    marca como respondido, repassa ao paciente, salva FAQ aprendido.
    """
    from app.database import SessionLocal
    from app.models import PendingEscalation

    with SessionLocal() as db:
        esc = (
            db.query(PendingEscalation)
            .filter_by(status="aguardando")
            .order_by(PendingEscalation.created_at.desc())
            .first()
        )
        if not esc:
            logger.warning("Resposta do Breno recebida mas sem escalacao pendente")
            return False

        esc.status = "respondido"
        esc.responded_at = datetime.now(BRT)
        esc.resposta_breno = texto_resposta
        telefone_paciente = esc.phone_e164
        pergunta = esc.pergunta_original
        db.commit()

    # Repassar ao paciente
    await meta_client.send_text(telefone_paciente, texto_resposta)
    logger.info("Resposta do Breno repassada ao paciente %s", telefone_paciente[-4:])

    # Salvar FAQ aprendido (D-11)
    try:
        from app.knowledge_base import salvar_faq_aprendido
        salvar_faq_aprendido(pergunta, texto_resposta)
    except Exception as e:
        logger.error("Falha ao salvar FAQ aprendido: %s", e)

    return True
```

Adicionar `enviar_lembretes_pendentes()` para ser chamado pelo scheduler:
```python
async def enviar_lembretes_pendentes(meta_client) -> int:
    """
    Verifica escalacoes pendentes e envia lembretes ao Breno + aviso ao paciente.
    Chamado pelo APScheduler a cada 5 minutos.

    Schedule de lembretes (D-09):
    - Primeira hora: a cada 15 min (reminder_count 0..3)
    - Apos primeira hora: a cada 1h

    Apos 1h sem resposta (D-10): avisa paciente.
    """
    from app.database import SessionLocal
    from app.models import PendingEscalation

    now = datetime.now(BRT)
    enviados = 0

    with SessionLocal() as db:
        pendentes = (
            db.query(PendingEscalation)
            .filter_by(status="aguardando")
            .filter(PendingEscalation.next_reminder_at <= now)
            .all()
        )

        for esc in pendentes:
            esc.reminder_count += 1
            tempo_decorrido = now - esc.created_at

            # D-10: apos 1h sem resposta, avisa paciente (apenas 1 vez, no reminder_count == 4)
            if esc.reminder_count == 4:
                try:
                    await meta_client.send_text(esc.phone_e164, _MSG_TIMEOUT_1H)
                except Exception as e:
                    logger.error("Falha ao avisar paciente timeout: %s", e)

            # Proximo lembrete
            if esc.reminder_count < 4:
                # Primeira hora: a cada 15 min
                esc.next_reminder_at = now + timedelta(minutes=15)
            else:
                # Apos primeira hora: a cada 1h
                esc.next_reminder_at = now + timedelta(hours=1)

            # Enviar lembrete ao Breno
            lembrete = f"⏰ *LEMBRETE #{esc.reminder_count}* — escalacao pendente ha {_formatar_tempo(tempo_decorrido)}\n\n{esc.contexto}"
            try:
                await meta_client.send_text(_NUMERO_INTERNO, lembrete)
                enviados += 1
            except Exception as e:
                logger.error("Falha ao enviar lembrete ao Breno: %s", e)

            db.commit()

    return enviados
```

Helper `_formatar_tempo(td: timedelta) -> str` que retorna "15 min", "1h30", etc.

**3. Detectar resposta do Breno no `app/webhook.py`:**

No handler de webhook, ANTES de chamar route_message, verificar se o remetente e o Breno (`_NUMERO_INTERNO`). Se for:
```python
from app.escalation import _NUMERO_INTERNO, processar_resposta_breno

# No process_message ou equivalente:
if sender_phone == _NUMERO_INTERNO or sender_phone == "5531992059211":
    # Mensagem do Breno — processar como resposta de escalacao
    await processar_resposta_breno(meta_client, text)
    return  # Nao rotear como mensagem normal
```

**4. Registrar job de lembretes no scheduler (`app/remarketing.py` ou `app/main.py`):**

Adicionar job ao APScheduler existente em `create_scheduler()`:
```python
# Job de lembretes de escalacao — a cada 5 minutos
scheduler.add_job(
    _check_escalation_reminders,
    "interval",
    minutes=5,
    id="escalation_reminders",
)
```

A funcao `_check_escalation_reminders()` cria um MetaAPIClient e chama `enviar_lembretes_pendentes()`.

**5. Criar `tests/test_escalation.py`:**

8 testes cobrindo todos os behaviors listados. Usar mocks para MetaAPIClient, SessionLocal, e PendingEscalation queries. Verificar especificamente que nenhuma mensagem enviada ao paciente contem "99205" ou "9211" (INTL-04).
  </action>
  <verify>
    <automated>python -m pytest tests/test_escalation.py -x -v</automated>
  </verify>
  <done>
    - 3 caminhos de escalacao funcionais (D-05, D-06, D-07)
    - Relay bidirecional: Breno recebe, responde, paciente recebe resposta
    - Lembretes ao Breno: 15min x4, depois 1h (D-09)
    - Timeout 1h: paciente avisado (D-10)
    - FAQ aprendido salvo na knowledge base (D-11)
    - Numero interno NUNCA exposto ao paciente (D-08, INTL-04)
    - Webhook detecta resposta do Breno
    - Todos os testes passam
  </done>
</task>

<task type="auto">
  <name>Task 2: Waiting indicator + FAQ aprendido persistente + alinhamento de tom</name>
  <files>app/agents/atendimento.py, app/knowledge_base.py, tests/test_behavior.py</files>
  <read_first>app/agents/atendimento.py, app/knowledge_base.py, agente-ana-documentacao-final.docx, docs/regras_remarcacao.md</read_first>
  <action>
**1. Adicionar `salvar_faq_aprendido()` ao `app/knowledge_base.py`:**

```python
# No final do modulo, antes de `kb = KnowledgeBase()`:

_FAQ_APRENDIDO_FILE = _KB_DIR / "faq_aprendido.json"

def salvar_faq_aprendido(pergunta: str, resposta: str) -> None:
    """
    Salva par pergunta/resposta aprendido do Breno na knowledge base.
    Persiste em arquivo JSON para sobreviver a deploys (D-11).
    """
    import json
    faq_list: list[dict] = []
    if _FAQ_APRENDIDO_FILE.exists():
        try:
            faq_list = json.loads(_FAQ_APRENDIDO_FILE.read_text(encoding="utf-8"))
        except Exception:
            faq_list = []

    # Evitar duplicatas exatas
    for item in faq_list:
        if item.get("pergunta", "").lower().strip() == pergunta.lower().strip():
            item["resposta"] = resposta  # atualizar resposta
            _FAQ_APRENDIDO_FILE.write_text(
                json.dumps(faq_list, ensure_ascii=False, indent=2), encoding="utf-8"
            )
            logger.info("FAQ aprendido atualizado: %s", pergunta[:50])
            return

    faq_list.append({
        "pergunta": pergunta,
        "resposta": resposta,
        "source": "breno_relay",
        "created_at": datetime.now(BRT).isoformat(),
    })
    _FAQ_APRENDIDO_FILE.write_text(
        json.dumps(faq_list, ensure_ascii=False, indent=2), encoding="utf-8"
    )
    logger.info("FAQ aprendido salvo: %s", pergunta[:50])
```

Adicionar import de `datetime` e `BRT` (de escalation ou definir localmente).

Atualizar `KnowledgeBase.faq_combinado()` para incluir FAQ aprendido:
```python
def faq_combinado(self) -> list[dict[str, str]]:
    resultado = list(self.faq_estatico)
    # FAQ minerado
    for item in self.faq_minerado:
        if item.get("frequency", 0) > 1 and item.get("suggested_answer"):
            resultado.append({"pergunta": item["question"], "resposta": item["suggested_answer"]})
    # FAQ aprendido (D-11)
    if _FAQ_APRENDIDO_FILE.exists():
        try:
            aprendido = json.loads(_FAQ_APRENDIDO_FILE.read_text(encoding="utf-8"))
            for item in aprendido:
                resultado.append({"pergunta": item["pergunta"], "resposta": item["resposta"]})
        except Exception as e:
            logger.error("Erro ao carregar FAQ aprendido: %s", e)
    return resultado
```

**2. Waiting indicator em `app/agents/atendimento.py` (D-21):**

O desafio: `processar()` e sincrono e retorna `list[str]`. Para enviar "Um instante" ANTES da operacao demorada, precisamos que o agente sinalize quais mensagens sao "pre-operacao".

Abordagem: adicionar atributo `self._pre_messages: list[str] = []` ao agente. Antes de chamar Dietbox/Rede, o metodo da etapa adiciona a mensagem de espera a `_pre_messages`. O router (Plan 01-02) envia `_pre_messages` ANTES de processar, e as respostas DEPOIS.

Alternativa mais simples: nos metodos `_etapa_agendamento()`, `_etapa_cadastro_dietbox()`, `_etapa_forma_pagamento()`, retornar a mensagem de espera como PRIMEIRO item da lista de respostas, e adicionar flag `self.needs_waiting_indicator = True` para o router saber que deve enviar a primeira mensagem antes de processar o resto.

Abordagem final (mais pragmatica): transformar os metodos que chamam servicos externos para retornar a mensagem de espera como primeiro item:

```python
# Em _etapa_agendamento:
def _etapa_agendamento(self, msg: str) -> list[str]:
    respostas = ["Um instante, por favor 💚"]  # D-21
    # ... codigo existente que chama dietbox_worker.consultar_slots_disponiveis()
    respostas.append(...)  # resultado
    return respostas
```

O router.py (refatorado no Plan 02) ja envia mensagens sequencialmente via `_enviar()`. A primeira mensagem ("Um instante") sera enviada antes das demais. Isso funciona porque `_enviar()` itera `for msg in mensagens` e envia uma por uma.

Aplicar em:
- `_etapa_agendamento()` — antes de `consultar_slots_disponiveis()`
- `_etapa_cadastro_dietbox()` — antes de `processar_agendamento()` ou `agendar_consulta()`
- `_etapa_forma_pagamento()` — antes de `gerar_link_pagamento()`

Tambem adicionar variacoes para nao repetir sempre a mesma frase:
```python
_WAITING_MESSAGES = [
    "Um instante, por favor 💚",
    "So um minutinho, ja verifico pra voce 💚",
    "Aguarda um instante que ja te respondo 💚",
]
```

Usar `random.choice(_WAITING_MESSAGES)` ou alternar com base na etapa.

**3. Alinhamento de tom e mensagens (D-18, D-19):**

Ler `agente-ana-documentacao-final.docx` para verificar o tom oficial. Atualizar constantes `MSG_*` em `app/agents/atendimento.py`:

- `MSG_BOAS_VINDAS`: deve usar "Eiii" ou "Oi!" conforme documentacao, tom informal e acolhedor
- `MSG_OBJETIVOS`: garantir que nao oferece Formulario proativamente
- Saudacoes devem usar emojis moderados: 💚, 😊, ✅, 📅
- Agendamento: NUNCA oferecer horario no mesmo dia. Ao oferecer na mesma semana, usar "tive uma desistencia amanha as X horas" (D-19)
- Verificar que todas as mensagens usam tom informal: "Perfeitoooo", "Obrigadaaa" em momentos de celebracao

Revisar e atualizar as constantes MSG_* para alinhar com a documentacao. Nao alterar a logica do FSM — apenas o texto das mensagens.

**4. Criar `tests/test_behavior.py`:**

```python
# Testes de alinhamento comportamental

def test_waiting_indicator_antes_de_dietbox():
    """D-21: primeira mensagem da lista e 'Um instante' ao buscar slots."""
    # Mock dietbox_worker.consultar_slots_disponiveis
    # Verificar que respostas[0] contem "instante" ou "minutinho"

def test_waiting_indicator_antes_de_pagamento():
    """D-21: waiting indicator ao gerar link de pagamento."""

def test_msg_boas_vindas_tom_informal():
    """D-18: saudacao usa tom informal conforme documentacao."""
    # Verificar que MSG_BOAS_VINDAS contem emoji e tom acolhedor

def test_agendamento_nunca_mesmo_dia():
    """D-19: slots oferecidos nunca incluem o dia atual."""

def test_formulario_nunca_oferecido_proativamente():
    """Regra: Formulario so mencionado se paciente perguntar."""
    # Verificar que nenhuma MSG_* contem "formulario" ou "Formulario"

def test_faq_aprendido_salva_e_carrega():
    """D-11: salvar_faq_aprendido persiste e faq_combinado inclui."""

def test_faq_aprendido_atualiza_duplicata():
    """D-11: mesma pergunta atualiza resposta em vez de duplicar."""
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_behavior.py tests/test_escalation.py -x -v</automated>
  </verify>
  <done>
    - Waiting indicator enviado antes de toda operacao Dietbox/Rede (D-21)
    - FAQ aprendido persistido em knowledge_base/faq_aprendido.json (D-11)
    - faq_combinado() inclui FAQ aprendido
    - Tom de mensagens alinhado com documentacao (D-18)
    - Agendamento nunca no mesmo dia (D-19)
    - Formulario nunca oferecido proativamente
    - Todos os testes passam
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Breno relay | Mensagem do Breno recebida pelo webhook e repassada ao paciente — confianca implicitamente alta (numero interno conhecido) |
| FAQ aprendido | Respostas do Breno salvas em arquivo JSON — conteudo nao sanitizado (Breno e confiavel) |
| Paciente | Nunca recebe numero interno; recebe apenas respostas filtradas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | app/escalation.py | mitigate | `_NUMERO_INTERNO` e constante privada; nenhuma funcao que envia ao paciente usa esse numero; testes verificam ausencia do numero em mensagens ao paciente |
| T-01-09 | Spoofing | app/webhook.py | accept | Mensagem do Breno identificada pelo sender_phone == _NUMERO_INTERNO; se alguem falsificar esse numero no Meta, o impacto e responder escalacao pendente — risco baixo pois webhook validado por HMAC (Phase 4) |
| T-01-10 | Tampering | app/knowledge_base.py faq_aprendido.json | accept | Arquivo gravado pelo processo da aplicacao; sem acesso externo. Conteudo e resposta do Breno (confiavel). Revisao manual possivel via leitura direta do JSON |
| T-01-11 | Denial of Service | app/escalation.py lembretes | mitigate | Scheduler roda a cada 5min com query limitada a pendentes; PendingEscalation com next_reminder_at impede envio duplicado; sem loop infinito |
</threat_model>

<verification>
1. `python -m pytest tests/test_escalation.py -x -v` — todos os testes de escalacao passam
2. `python -m pytest tests/test_behavior.py -x -v` — testes de waiting indicator e alinhamento passam
3. `grep -r "99205\|9211" app/escalation.py` — aparece apenas em `_NUMERO_INTERNO` (constante interna)
4. `grep -r "99205\|9211" tests/test_escalation.py` — testes verificam que numero NAO aparece em mensagens ao paciente
5. `python -m pytest tests/ -q` — suite completa sem regressoes
</verification>

<success_criteria>
- 3 caminhos de escalacao funcionais (paciente cadastrado → contato Thaynara, lead → relay Breno, Ana nao sabe → relay Breno)
- Relay bidirecional: webhook detecta resposta do Breno e repassa ao paciente
- Lembretes ao Breno nos intervalos corretos (15min x4, depois 1h)
- Paciente avisado apos 1h sem resposta
- FAQ aprendido persistido e incluido em faq_combinado()
- Waiting indicator antes de toda operacao demorada
- Tom e mensagens alinhados com documentacao oficial
- Numero interno nunca exposto ao paciente
- Todos os testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/01-intelig-ncia-conversacional/01-03-SUMMARY.md`
</output>
