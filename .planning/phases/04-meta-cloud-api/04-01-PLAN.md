---
phase: 04-meta-cloud-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/webhook.py
  - app/meta_api.py
  - tests/test_webhook.py
  - tests/test_meta_api.py
autonomous: true
requirements: [META-04, META-02]

must_haves:
  truths:
    - "Mesma mensagem entregue duas vezes pelo Meta nao cria dois processamentos — Redis SET NX bloqueia a segunda"
    - "Se Redis estiver indisponivel, webhook continua funcionando com dedup por DB existente"
    - "MetaAPIClient() chamado sem argumentos le env vars automaticamente em vez de quebrar com TypeError"
  artifacts:
    - path: "app/webhook.py"
      provides: "Redis dedup atomica via SET NX antes de qualquer processamento"
      contains: "dedup:msg:"
    - path: "app/meta_api.py"
      provides: "MetaAPIClient.__init__ com args opcionais que le env vars"
      contains: "WHATSAPP_PHONE_NUMBER_ID"
    - path: "tests/test_webhook.py"
      provides: "Testes de dedup Redis SET NX, graceful degradation"
      contains: "test_dedup_redis"
    - path: "tests/test_meta_api.py"
      provides: "Teste MetaAPIClient sem args"
      contains: "test_client_no_args"
  key_links:
    - from: "app/webhook.py"
      to: "redis.asyncio"
      via: "SET NX com key dedup:msg:{meta_message_id}"
      pattern: "redis.*set.*nx"
    - from: "app/meta_api.py"
      to: "os.environ"
      via: "MetaAPIClient.__init__ le WHATSAPP_PHONE_NUMBER_ID e WHATSAPP_TOKEN"
      pattern: "os\\.environ\\.get"
---

<objective>
Implementar deduplicacao atomica de mensagens via Redis SET NX e corrigir bug de instanciacao do MetaAPIClient.

Purpose: Eliminar race condition na dedup por DB (GAP-1) e corrigir TypeError em producao quando MetaAPIClient() e chamado sem argumentos (GAP-3).
Output: webhook.py com Redis dedup antes do processamento + MetaAPIClient com args opcionais + testes cobrindo ambos cenarios.
</objective>

<execution_context>
@C:/Users/Breno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Breno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-meta-cloud-api/04-RESEARCH.md

<interfaces>
From app/webhook.py (current process_message):
```python
async def process_message(message: dict, metadata: dict):
    # Dedup por DB: db.query(Message).filter_by(meta_message_id=meta_id).first()
    # Bug: se dois workers rodarem em paralelo, ambos passam pelo filter_by
```

From app/meta_api.py (current MetaAPIClient):
```python
class MetaAPIClient:
    def __init__(self, phone_number_id: str, access_token: str):
        # Ambos args sao obrigatorios — router.py:71 chama MetaAPIClient() sem args
```

From app/state_manager.py (padrao Redis existente):
```python
self._client = aioredis.Redis.from_url(redis_url, decode_responses=True)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de dedup Redis + MetaAPIClient sem args (Wave 0)</name>
  <files>tests/test_webhook.py, tests/test_meta_api.py</files>
  <read_first>
    - tests/test_webhook.py (estado atual — 3 testes existentes)
    - tests/test_meta_api.py (estado atual — 4 testes existentes)
    - app/webhook.py (entender process_message atual)
    - app/meta_api.py (entender MetaAPIClient.__init__ atual)
    - .planning/phases/04-meta-cloud-api/04-RESEARCH.md (secao Code Examples — dedup pattern)
  </read_first>
  <behavior>
    - test_dedup_redis_blocks_duplicate: mock redis.asyncio SET NX retornando None na segunda chamada; segunda mensagem com mesmo meta_message_id nao chama route_message
    - test_dedup_redis_allows_first: mock redis SET NX retornando True; primeira mensagem E processada normalmente
    - test_dedup_graceful_degradation: mock redis SET NX levantando ConnectionError; mensagem e processada (fail open) e dedup cai para DB existente
    - test_client_no_args_reads_env: criar MetaAPIClient() sem args com env vars mockadas; assert _phone_id == env value e Authorization header contem token
  </behavior>
  <action>
Adicionar 4 novos testes aos arquivos existentes:

Em `tests/test_webhook.py`, adicionar:
1. `test_dedup_redis_blocks_duplicate` — mockar `_is_duplicate_message` retornando True; chamar `process_message()` com fixture de mensagem; assert que `route_message` NAO foi chamado.
2. `test_dedup_redis_allows_first` — mockar `_is_duplicate_message` retornando False; chamar `process_message()` com fixture; assert que `route_message` FOI chamado.
3. `test_dedup_graceful_degradation` — mockar `_is_duplicate_message` levantando `Exception("Redis down")`; assert que `route_message` FOI chamado (fail open).

Em `tests/test_meta_api.py`, adicionar:
4. `test_client_no_args_reads_env` — com `patch.dict(os.environ, {"WHATSAPP_PHONE_NUMBER_ID": "env_phone_id", "WHATSAPP_TOKEN": "env_token"})`, instanciar `MetaAPIClient()` sem args; assert `client._phone_id == "env_phone_id"` e `"Bearer env_token" in client._headers["Authorization"]`.

NOTA: Os testes de webhook devem mockar tanto `_is_duplicate_message` quanto `route_message` e demais dependencias (SessionLocal, Contact, etc.) para isolar a logica de dedup. Usar `unittest.mock.patch` e `pytest.mark.asyncio`.

Todos os 4 testes DEVEM falhar na primeira execucao (funcoes `_is_duplicate_message` ainda nao existe, MetaAPIClient ainda exige args).
  </action>
  <verify>
    <automated>python -m pytest tests/test_webhook.py tests/test_meta_api.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "test_dedup_redis_blocks_duplicate" tests/test_webhook.py retorna match
    - grep "test_dedup_redis_allows_first" tests/test_webhook.py retorna match
    - grep "test_dedup_graceful_degradation" tests/test_webhook.py retorna match
    - grep "test_client_no_args_reads_env" tests/test_meta_api.py retorna match
    - Testes novos existem e FALHAM (RED phase do TDD) — esperado neste task
  </acceptance_criteria>
  <done>4 novos testes adicionados aos arquivos existentes, todos falhando (funcoes referenciadas ainda nao existem)</done>
</task>

<task type="auto">
  <name>Task 2: Redis dedup atomica em webhook.py</name>
  <files>app/webhook.py</files>
  <read_first>
    - app/webhook.py (estado atual — process_message com dedup por DB)
    - app/state_manager.py (padrao de uso de redis.asyncio no projeto)
    - .planning/phases/04-meta-cloud-api/04-RESEARCH.md (secao "Padrao 1: Redis SET NX")
  </read_first>
  <action>
Adicionar funcao `_is_duplicate_message()` no topo de `app/webhook.py` e chamar no inicio de `process_message()`:

1. Adicionar imports no topo:
```python
import redis.asyncio as aioredis
```

2. Constante:
```python
_DEDUP_TTL = 14400  # 4 horas em segundos
```

3. Funcao (antes de process_message):
```python
async def _is_duplicate_message(meta_message_id: str) -> bool:
    """
    Retorna True se mensagem ja foi processada (duplicata).
    Redis SET NX EX atomico. Graceful degradation: False se Redis falhar.
    """
    redis_url = os.environ.get("REDIS_URL", "redis://redis:6379/0")
    try:
        r = aioredis.Redis.from_url(redis_url, decode_responses=True)
        key = f"dedup:msg:{meta_message_id}"
        result = await r.set(key, "1", nx=True, ex=_DEDUP_TTL)
        await r.aclose()
        return result is None  # None = key ja existia = duplicata
    except Exception as e:
        logger.warning("Redis dedup indisponivel: %s — prosseguindo sem dedup", e)
        return False  # fail open
```

4. No inicio de `process_message()`, ANTES do bloco `with SessionLocal() as db:`, adicionar:
```python
    if await _is_duplicate_message(meta_id):
        logger.debug("Dedup Redis: mensagem %s ja processada, ignorando", meta_id)
        return
```

5. Manter a dedup por DB existente como segunda camada de seguranca (nao remover o `existing = db.query(Message).filter_by(meta_message_id=meta_id).first()`).
  </action>
  <verify>
    <automated>python -m pytest tests/test_webhook.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "import redis.asyncio as aioredis" app/webhook.py retorna match
    - grep "_DEDUP_TTL = 14400" app/webhook.py retorna match
    - grep "dedup:msg:" app/webhook.py retorna match
    - grep "_is_duplicate_message" app/webhook.py retorna pelo menos 2 matches (definicao + chamada)
    - grep "fail open" app/webhook.py retorna match (comentario no except)
    - Dedup por DB (filter_by meta_message_id) ainda presente como camada extra
    - Testes test_dedup_redis_* passam (GREEN)
  </acceptance_criteria>
  <done>Redis SET NX dedup implementado em webhook.py; primeira mensagem processada, duplicata ignorada; Redis down = fail open para DB dedup</done>
</task>

<task type="auto">
  <name>Task 3: Fix MetaAPIClient.__init__ para args opcionais + limpar router.py</name>
  <files>app/meta_api.py, app/router.py</files>
  <read_first>
    - app/meta_api.py (MetaAPIClient.__init__ atual — args obrigatorios)
    - app/router.py (linha 71 — MetaAPIClient() sem args)
    - app/webhook.py (linhas 109-112 — MetaAPIClient com args explicitos)
    - .planning/phases/04-meta-cloud-api/04-RESEARCH.md (secao "Fix: MetaAPIClient sem args")
  </read_first>
  <action>
1. Em `app/meta_api.py`, modificar `MetaAPIClient.__init__` para aceitar args opcionais com fallback para env vars:

```python
import os  # adicionar no topo se nao existir

class MetaAPIClient:
    def __init__(
        self,
        phone_number_id: str | None = None,
        access_token: str | None = None,
    ):
        self._phone_id = phone_number_id or os.environ.get("WHATSAPP_PHONE_NUMBER_ID", "")
        token = access_token or os.environ.get("WHATSAPP_TOKEN", "")
        self._headers = {
            "Authorization": f"Bearer {token}",
            "Content-Type": "application/json",
        }
```

2. Em `app/router.py`, simplificar linha 71 de:
```python
    meta = MetaAPIClient()
```
Para ficar sem mudanca (agora funciona porque __init__ le env vars).

3. Em `app/webhook.py`, a instanciacao nas linhas 109-112 com args explicitos CONTINUA funcionando (args tem precedencia sobre env vars). Nao alterar.

4. Rodar suite completa para garantir que testes existentes (que passam args) continuam verdes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_webhook.py tests/test_meta_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "phone_number_id: str | None = None" app/meta_api.py retorna match
    - grep "access_token: str | None = None" app/meta_api.py retorna match
    - grep 'os.environ.get("WHATSAPP_PHONE_NUMBER_ID"' app/meta_api.py retorna match
    - grep 'os.environ.get("WHATSAPP_TOKEN"' app/meta_api.py retorna match
    - test_client_no_args_reads_env passa (GREEN)
    - Todos os testes existentes em test_meta_api.py continuam passando (args explicitos tem precedencia)
    - python -m pytest tests/ -q passa completo (full suite)
  </acceptance_criteria>
  <done>MetaAPIClient aceita chamada sem args (le env vars); chamada com args continua funcionando; router.py MetaAPIClient() nao quebra mais</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Meta webhook -> app | Requisicoes HTTP externas da Meta chegam ao webhook; HMAC valida autenticidade |
| Redis -> app | Redis e infraestrutura interna; falha nao deve bloquear processamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | webhook.py process_message | mitigate | Redis SET NX atomico impede processamento duplicado (key: `dedup:msg:{meta_message_id}`, TTL: 14400s) |
| T-04-02 | Denial of Service | webhook.py _is_duplicate_message | mitigate | Graceful degradation: se Redis cair, retorna False (fail open), cai para dedup DB existente |
| T-04-03 | Spoofing | webhook.py receive_webhook | accept | HMAC verify_signature ja implementado e testado — nao alterado neste plan |
| T-04-04 | Information Disclosure | meta_api.py MetaAPIClient | mitigate | Env vars lidos no __init__, nunca logados; token so aparece no header Authorization |
</threat_model>

<verification>
1. `python -m pytest tests/test_webhook.py -q` — todos os testes passam (3 antigos + 3 novos)
2. `python -m pytest tests/test_meta_api.py -q` — todos os testes passam (4 antigos + 1 novo)
3. `python -m pytest tests/ -q` — full suite verde
4. `grep "dedup:msg:" app/webhook.py` — confirma key pattern
5. `grep "_is_duplicate_message" app/webhook.py` — funcao existe e e chamada
6. `grep "phone_number_id: str | None = None" app/meta_api.py` — args opcionais
</verification>

<success_criteria>
- Mensagem duplicada (mesmo meta_message_id) nao e processada duas vezes
- Redis indisponivel nao bloqueia o webhook (graceful degradation)
- MetaAPIClient() sem argumentos funciona lendo env vars
- Todos os testes existentes continuam passando
- 4 novos testes cobrem dedup Redis e MetaAPIClient sem args
</success_criteria>

<output>
After completion, create `.planning/phases/04-meta-cloud-api/04-01-SUMMARY.md`
</output>
