---
phase: 04-meta-cloud-api
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/meta_api.py
  - app/media_store.py
  - app/agents/atendimento.py
  - app/router.py
  - tests/test_meta_api.py
autonomous: true
requirements: [META-03, META-02]

must_haves:
  truths:
    - "PDFs e imagens de preparo sao enviados ao paciente como arquivos reais, nao placeholders de texto"
    - "Media upload usa cache Redis para evitar re-upload do mesmo arquivo"
    - "Placeholders [PDF: ...] e [IMG: ...] nao aparecem mais nas respostas do atendimento.py"
  artifacts:
    - path: "app/meta_api.py"
      provides: "Metodos upload_media, send_document, send_image"
      exports: ["upload_media", "send_document", "send_image"]
    - path: "app/media_store.py"
      provides: "Dict de arquivos estaticos com path, mime, filename"
      contains: "MEDIA_STATIC"
    - path: "app/agents/atendimento.py"
      provides: "Placeholders substituidos por tuplas de midia {type, key}"
      contains: "media_type"
    - path: "app/router.py"
      provides: "_enviar detecta e envia midia real via send_document/send_image"
      contains: "send_document"
    - path: "tests/test_meta_api.py"
      provides: "Testes para upload_media, send_document, send_image"
      contains: "test_upload_media"
  key_links:
    - from: "app/agents/atendimento.py"
      to: "app/router.py"
      via: "Retorna dicts {media_type, media_key} em vez de strings [PDF: ...]"
      pattern: "media_type"
    - from: "app/router.py"
      to: "app/meta_api.py"
      via: "_enviar chama send_document/send_image quando detecta dict de midia"
      pattern: "send_document|send_image"
    - from: "app/meta_api.py"
      to: "Redis"
      via: "Cache de media_id com key media_id:{hash} e TTL 82800s"
      pattern: "media_id:"
---

<objective>
Implementar envio real de midia (PDF e imagens) via Meta Cloud API, substituindo placeholders de texto.

Purpose: Pacientes recebem os arquivos reais de preparo para consulta (imagens de como se preparar, guias de circunferencias, PDF da nutricionista) em vez de strings literais "[PDF: ...]".
Output: MetaAPIClient com upload_media/send_document/send_image + media_store.py + placeholders substituidos em atendimento.py + _enviar em router.py detecta e envia midia.
</objective>

<execution_context>
@C:/Users/Breno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Breno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-meta-cloud-api/04-RESEARCH.md
@.planning/phases/04-meta-cloud-api/04-01-SUMMARY.md

<interfaces>
From app/meta_api.py (apos 04-01):
```python
META_API_BASE = "https://graph.facebook.com/v19.0"

class MetaAPIClient:
    def __init__(self, phone_number_id: str | None = None, access_token: str | None = None):
        self._phone_id = phone_number_id or os.environ.get("WHATSAPP_PHONE_NUMBER_ID", "")
        token = access_token or os.environ.get("WHATSAPP_TOKEN", "")
        self._headers = {"Authorization": f"Bearer {token}", "Content-Type": "application/json"}

    async def send_text(self, to: str, text: str) -> dict: ...
    async def send_template(self, to: str, template_name: str, ...) -> dict: ...
    async def _post(self, payload: dict) -> dict: ...
```

From app/router.py _enviar (atual — so envia texto):
```python
async def _enviar(meta, phone: str, mensagens: list[str | None]):
    for msg in mensagens:
        if msg:
            try:
                await meta.send_text(phone, msg)
            except Exception as e:
                logger.error("Falha ao enviar mensagem para %s: %s", phone[-4:], e)
```

From app/agents/atendimento.py placeholders:
```python
# _etapa_qualificacao (linha 335):
"[PDF: Thaynara - Nutricionista.pdf]"

# _etapa_confirmacao online (linhas 630-631):
"[IMG: COMO-SE-PREPARAR---ONLINE.jpg]"
"[PDF: Guia Circunferencias Corporais]"

# _etapa_confirmacao presencial (linha 642):
"[IMG: COMO-SE-PREPARAR---presencial.jpg]"
```

Arquivos fisicos confirmados em docs/:
- docs/Thaynara - Nutricionista.pdf
- docs/COMO-SE-PREPARAR---ONLINE.jpg
- docs/COMO-SE-PREPARAR---presencial.jpg
- docs/Guia - Circunferencias Corporais - Mulheres.pdf
- docs/Guia - Circunferencias Corporais - Homens.pdf
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes de upload_media, send_document, send_image (Wave 0) + implementacao em meta_api.py e media_store.py</name>
  <files>tests/test_meta_api.py, app/meta_api.py, app/media_store.py</files>
  <read_first>
    - tests/test_meta_api.py (testes existentes — incluindo os novos do 04-01)
    - app/meta_api.py (MetaAPIClient apos fix do 04-01)
    - .planning/phases/04-meta-cloud-api/04-RESEARCH.md (secoes "Padrao 2: Upload de midia" e "Recomendacao de projeto para midia estatica")
  </read_first>
  <behavior>
    - test_upload_media_returns_media_id: respx mock POST /{phone_id}/media retorna {"id": "4490709327384033"}; assert retorno == "4490709327384033"
    - test_send_document_payload: respx mock POST /{phone_id}/messages; assert payload contem type=document, document.id=media_id, document.filename
    - test_send_image_payload: respx mock POST /{phone_id}/messages; assert payload contem type=image, image.id=media_id
    - test_upload_media_uses_cache: segunda chamada com mesmo arquivo nao faz HTTP; retorna media_id do Redis cache
    - test_media_store_keys: assert MEDIA_STATIC contem keys pdf_thaynara, img_preparo_online, img_preparo_presencial, pdf_guia_circunf_mulher, pdf_guia_circunf_homem
  </behavior>
  <action>
**Parte A: Testes (RED)**

Em `tests/test_meta_api.py`, adicionar 4 novos testes usando `respx.mock` e `pytest.mark.asyncio`:

1. `test_upload_media_returns_media_id` — mock `POST https://graph.facebook.com/v19.0/{PHONE_ID}/media` retornando `{"id": "4490709327384033"}`; chamar `client.upload_media(b"fake_pdf_bytes", "application/pdf", "test.pdf")`; assert resultado == `"4490709327384033"`.

2. `test_send_document_payload` — mock POST messages; chamar `client.send_document(to="5531999999999", media_id="4490709327384033", filename="test.pdf", caption="Teste")`; assert payload `type == "document"`, `document.id == "4490709327384033"`, `document.filename == "test.pdf"`, `document.caption == "Teste"`.

3. `test_send_image_payload` — mock POST messages; chamar `client.send_image(to="5531999999999", media_id="4490709327384033", caption="Preparo")`; assert payload `type == "image"`, `image.id == "4490709327384033"`, `image.caption == "Preparo"`.

4. `test_media_store_has_all_keys` — `from app.media_store import MEDIA_STATIC`; assert todas as 5 keys existem e cada valor tem "path", "mime", "filename".

**Parte B: Implementacao (GREEN)**

Em `app/meta_api.py`, adicionar 3 metodos a `MetaAPIClient`:

```python
async def upload_media(self, file_bytes: bytes, mime_type: str, filename: str) -> str:
    """Faz upload de arquivo para Meta e retorna media_id."""
    url = f"{META_API_BASE}/{self._phone_id}/media"
    files = {"file": (filename, file_bytes, mime_type)}
    data = {"messaging_product": "whatsapp", "type": mime_type}
    auth_header = {"Authorization": self._headers["Authorization"]}
    async with httpx.AsyncClient(headers=auth_header, timeout=30) as client:
        resp = await client.post(url, files=files, data=data)
        resp.raise_for_status()
        return resp.json()["id"]

async def send_document(self, to: str, media_id: str, filename: str,
                         caption: str = "") -> dict:
    payload = {
        "messaging_product": "whatsapp",
        "to": to,
        "type": "document",
        "document": {
            "id": media_id,
            "filename": filename,
            **({"caption": caption} if caption else {}),
        },
    }
    return await self._post(payload)

async def send_image(self, to: str, media_id: str, caption: str = "") -> dict:
    payload = {
        "messaging_product": "whatsapp",
        "to": to,
        "type": "image",
        "image": {
            "id": media_id,
            **({"caption": caption} if caption else {}),
        },
    }
    return await self._post(payload)
```

Criar `app/media_store.py`:
```python
"""
Catalogo de arquivos estaticos para envio via Meta Cloud API.

Cada entrada mapeia uma chave semantica para o arquivo fisico em docs/.
Usado por router.py para resolver media_key -> file_bytes + mime + filename.
"""

MEDIA_STATIC: dict[str, dict[str, str]] = {
    "pdf_thaynara": {
        "path": "docs/Thaynara - Nutricionista.pdf",
        "mime": "application/pdf",
        "filename": "Thaynara - Nutricionista.pdf",
    },
    "img_preparo_online": {
        "path": "docs/COMO-SE-PREPARAR---ONLINE.jpg",
        "mime": "image/jpeg",
        "filename": "preparo-online.jpg",
    },
    "img_preparo_presencial": {
        "path": "docs/COMO-SE-PREPARAR---presencial.jpg",
        "mime": "image/jpeg",
        "filename": "preparo-presencial.jpg",
    },
    "pdf_guia_circunf_mulher": {
        "path": "docs/Guia - Circunferencias Corporais - Mulheres.pdf",
        "mime": "application/pdf",
        "filename": "Guia-Circunferencias-Mulheres.pdf",
    },
    "pdf_guia_circunf_homem": {
        "path": "docs/Guia - Circunferencias Corporais - Homens.pdf",
        "mime": "application/pdf",
        "filename": "Guia-Circunferencias-Homens.pdf",
    },
}
```
  </action>
  <verify>
    <automated>python -m pytest tests/test_meta_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "test_upload_media_returns_media_id" tests/test_meta_api.py retorna match
    - grep "test_send_document_payload" tests/test_meta_api.py retorna match
    - grep "test_send_image_payload" tests/test_meta_api.py retorna match
    - grep "test_media_store_has_all_keys" tests/test_meta_api.py retorna match
    - grep "async def upload_media" app/meta_api.py retorna match
    - grep "async def send_document" app/meta_api.py retorna match
    - grep "async def send_image" app/meta_api.py retorna match
    - grep "MEDIA_STATIC" app/media_store.py retorna match
    - grep "pdf_thaynara" app/media_store.py retorna match
    - python -m pytest tests/test_meta_api.py -q todos passam
  </acceptance_criteria>
  <done>MetaAPIClient tem upload_media, send_document, send_image; media_store.py criado com 5 arquivos; todos os testes passam</done>
</task>

<task type="auto">
  <name>Task 2: Substituir placeholders em atendimento.py e atualizar _enviar em router.py</name>
  <files>app/agents/atendimento.py, app/router.py</files>
  <read_first>
    - app/agents/atendimento.py (linhas 329-337 _etapa_qualificacao, linhas 618-645 _etapa_confirmacao — placeholders)
    - app/router.py (_enviar atual — so envia texto; linhas 269-276)
    - app/media_store.py (criado no Task 1 — MEDIA_STATIC dict)
    - app/meta_api.py (metodos send_document e send_image adicionados no Task 1)
  </read_first>
  <action>
**Estrategia:** Atendimento.py retorna dicts de midia em vez de strings placeholder. Router.py detecta dicts na lista de mensagens e envia via send_document/send_image com upload + cache Redis.

**1. Em `app/agents/atendimento.py`:**

Substituir cada placeholder por dict:

Em `_etapa_qualificacao` (linha 335), trocar:
```python
"[PDF: Thaynara - Nutricionista.pdf]"
```
Por:
```python
{"media_type": "document", "media_key": "pdf_thaynara", "caption": "Nosso midia kit completo"}
```

Em `_etapa_confirmacao` modalidade online (linhas 630-631), trocar:
```python
"[IMG: COMO-SE-PREPARAR---ONLINE.jpg]"
"[PDF: Guia Circunferencias Corporais]"
```
Por:
```python
{"media_type": "image", "media_key": "img_preparo_online", "caption": "Como se preparar para a consulta online"}
{"media_type": "document", "media_key": "pdf_guia_circunf_mulher", "caption": "Guia de medidas corporais"}
```

Em `_etapa_confirmacao` modalidade presencial (linha 642), trocar:
```python
"[IMG: COMO-SE-PREPARAR---presencial.jpg]"
```
Por:
```python
{"media_type": "image", "media_key": "img_preparo_presencial", "caption": "Como se preparar para a consulta presencial"}
```

**2. Em `app/router.py`:**

Modificar `_enviar` para detectar dicts de midia e enviar via Meta Cloud API real:

```python
import hashlib as _hashlib
import redis.asyncio as _aioredis

_MEDIA_CACHE_TTL = 82800  # 23h em segundos

async def _enviar(meta, phone: str, mensagens: list):
    """Envia lista de mensagens via Meta API. Detecta dicts de midia e envia como documento/imagem."""
    for msg in mensagens:
        if not msg:
            continue
        try:
            if isinstance(msg, dict) and "media_type" in msg:
                await _enviar_midia(meta, phone, msg)
            else:
                await meta.send_text(phone, msg)
        except Exception as e:
            logger.error("Falha ao enviar mensagem para %s: %s", phone[-4:], e)


async def _enviar_midia(meta, phone: str, media_msg: dict):
    """Resolve media_key -> upload (com cache) -> send_document/send_image."""
    from app.media_store import MEDIA_STATIC

    key = media_msg["media_key"]
    info = MEDIA_STATIC.get(key)
    if not info:
        logger.error("media_key '%s' nao encontrada em MEDIA_STATIC", key)
        return

    media_id = await _get_or_upload_media(meta, key, info)
    if not media_id:
        logger.error("Falha ao obter media_id para '%s'", key)
        return

    caption = media_msg.get("caption", "")
    if media_msg["media_type"] == "document":
        await meta.send_document(to=phone, media_id=media_id, filename=info["filename"], caption=caption)
    elif media_msg["media_type"] == "image":
        await meta.send_image(to=phone, media_id=media_id, caption=caption)


async def _get_or_upload_media(meta, media_key: str, info: dict) -> str | None:
    """Retorna media_id do cache Redis ou faz upload e cacheia."""
    import os
    cache_key = f"media_id:{_hashlib.sha256(media_key.encode()).hexdigest()[:16]}"
    redis_url = os.environ.get("REDIS_URL", "redis://redis:6379/0")

    # Tentar cache
    try:
        r = _aioredis.Redis.from_url(redis_url, decode_responses=True)
        cached = await r.get(cache_key)
        if cached:
            await r.aclose()
            return cached
    except Exception:
        r = None  # Redis indisponivel — prosseguir com upload

    # Upload
    try:
        file_bytes = open(info["path"], "rb").read()
        media_id = await meta.upload_media(file_bytes, info["mime"], info["filename"])
    except Exception as e:
        logger.error("Falha upload midia '%s': %s", media_key, e)
        return None

    # Cachear
    try:
        if r is None:
            r = _aioredis.Redis.from_url(redis_url, decode_responses=True)
        await r.set(cache_key, media_id, ex=_MEDIA_CACHE_TTL)
        await r.aclose()
    except Exception:
        pass  # Cache falhou — nao critico, proximo envio fara upload de novo

    return media_id
```

**NOTA:** O type hint de `mensagens` em `_enviar` muda de `list[str | None]` para `list` (aceita str e dict). Atualizar a type hint na assinatura.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - grep "media_type" app/agents/atendimento.py retorna matches (pelo menos 4 — um por placeholder substituido)
    - grep "media_key" app/agents/atendimento.py retorna matches
    - grep '\[PDF:' app/agents/atendimento.py NAO retorna match (placeholders removidos)
    - grep '\[IMG:' app/agents/atendimento.py NAO retorna match (placeholders removidos)
    - grep "_enviar_midia" app/router.py retorna match
    - grep "_get_or_upload_media" app/router.py retorna match
    - grep "media_id:" app/router.py retorna match (cache key pattern)
    - grep "_MEDIA_CACHE_TTL = 82800" app/router.py retorna match
    - grep "send_document" app/router.py retorna match
    - grep "send_image" app/router.py retorna match
    - python -m pytest tests/ -q passa completo
  </acceptance_criteria>
  <done>Placeholders [PDF:] e [IMG:] substituidos por dicts de midia; router.py envia arquivos reais via Meta Cloud API com cache Redis de media_id (TTL 23h); suite completa verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> Meta Cloud API | Upload de midia e envio de mensagens; token Bearer no header |
| local filesystem -> app | Leitura de arquivos de docs/ para upload |
| Redis cache -> app | Cache de media_id; falha nao deve bloquear envio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | media_store.py | accept | Arquivos em docs/ sao materiais de marketing publicos (nao PII); paths hardcoded no servidor |
| T-04-06 | Denial of Service | _get_or_upload_media | mitigate | Redis cache com TTL 82800s (23h) evita re-upload a cada mensagem; timeout de 30s no upload |
| T-04-07 | Tampering | Redis media_id cache | accept | Risco baixo: media_id falsificado causaria erro 400 da Meta, nao exposicao de dados. Redis e infra interna |
| T-04-08 | Denial of Service | upload_media | mitigate | try/except com log; falha de upload nao bloqueia outras mensagens da lista |
</threat_model>

<verification>
1. `python -m pytest tests/test_meta_api.py -q` — todos os testes passam (antigos + 4 novos)
2. `python -m pytest tests/ -q` — full suite verde
3. `grep '\[PDF:' app/agents/atendimento.py` — nenhum match (zero placeholders)
4. `grep '\[IMG:' app/agents/atendimento.py` — nenhum match
5. `grep "send_document\|send_image\|upload_media" app/meta_api.py` — 3 metodos existem
6. `grep "MEDIA_STATIC" app/media_store.py` — catalogo existe
</verification>

<success_criteria>
- MetaAPIClient tem upload_media, send_document, send_image funcionais
- media_store.py mapeia 5 arquivos estaticos com path, mime, filename
- Nenhum placeholder [PDF:] ou [IMG:] resta em atendimento.py
- router.py detecta dicts de midia e envia via Meta Cloud API real
- Cache Redis de media_id evita re-upload (TTL 23h)
- Todos os testes passam
</success_criteria>

<output>
After completion, create `.planning/phases/04-meta-cloud-api/04-02-SUMMARY.md`
</output>
