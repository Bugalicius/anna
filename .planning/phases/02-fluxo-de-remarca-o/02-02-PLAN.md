---
phase: 02-fluxo-de-remarca-o
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/agents/retencao.py
  - tests/test_retencao.py
autonomous: true
requirements:
  - REMC-02
  - REMC-05
  - REMC-06
must_haves:
  truths:
    - "Ana sempre oferece exatamente 3 opções de horário ao paciente"
    - "A primeira opção é o slot mais próximo da preferência declarada (dia + hora); as outras 2 são os próximos disponíveis"
    - "Se não houver slot na preferência, Ana oferece as 3 primeiras opções disponíveis sem mencionar preferência"
    - "Após rejeição dos 3 slots, Ana oferece mais 3 do pool restante (segunda rodada)"
    - "Após rejeição da segunda rodada, Ana declara 'perda de retorno' e oferece nova consulta"
    - "Opções em dias diferentes são preferidas — nunca 3 slots no mesmo dia"
  artifacts:
    - path: "app/agents/retencao.py"
      provides: "_priorizar_slots() substituindo _selecionar_slots_dias_diferentes(), etapas rodada_2 e perda_retorno, rodada_negociacao counter"
      contains: "_priorizar_slots, MSG_PERDA_RETORNO, MSG_SEGUNDA_RODADA, rodada_negociacao"
    - path: "tests/test_retencao.py"
      provides: "Testes para priorização de slots, 2 rodadas de negociação e perda de retorno"
  key_links:
    - from: "app/agents/retencao.py:_fluxo_remarcacao:etapa oferecendo_slots"
      to: "app/agents/retencao.py:_priorizar_slots"
      via: "chamada com pool completo e preferência do paciente"
      pattern: "_priorizar_slots"
    - from: "app/agents/retencao.py:etapa oferecendo_slots — rejeição"
      to: "app/agents/retencao.py:rodada_negociacao counter"
      via: "incremento em rodada_negociacao antes de oferecer próximos slots"
      pattern: "rodada_negociacao"
---

<objective>
Implementar o algoritmo de priorização de horários (D-09 a D-13) e as 2 rodadas de negociação com fallback de perda de retorno (D-14 a D-19).

Purpose: O código atual usa _selecionar_slots_dias_diferentes() que ignora a preferência do paciente e não implementa rodadas de negociação. O resultado é que a Ana oferece horários aleatórios e nunca tenta negociar — pacientes que não gostam das opções ficam sem resposta.

Output:
- _priorizar_slots() com lógica de: 1 slot preferido + 2 próximos disponíveis em dias diferentes
- Etapa "oferecendo_slots" expandida: detecta rejeição, incrementa rodada_negociacao, oferece próximos 3
- Etapa "perda_retorno": declarada após 2 rodadas sem acordo, oferece nova consulta
- Mensagens fixas per D-26/D-27 (tom informal, emojis moderados)
- Testes cobrindo os algoritmos e o fluxo de negociação
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fluxo-de-remarca-o/02-CONTEXT.md
@.planning/phases/02-fluxo-de-remarca-o/02-RESEARCH.md
@docs/regras_remarcacao.md
@app/agents/retencao.py
@tests/test_retencao.py

<interfaces>
<!-- Contratos que este plano consome — produzidos pelo Plan 02-01 ou já existentes -->

De app/agents/retencao.py (após Plan 02-01):
```python
class AgenteRetencao:
    # Novos campos adicionados em 02-01:
    self.tipo_remarcacao: str | None   # "retorno" | "nova_consulta"
    self.rodada_negociacao: int        # 0, 1 ou 2
    self._slots_pool: list[dict]       # pool completo de slots da janela
    self._slots_oferecidos: list[dict] # os 3 slots atualmente na tela do paciente

# Formato dos slots (de consultar_slots_disponiveis):
# {"datetime": "2026-04-14T09:00:00", "data_fmt": "segunda, 14/04", "hora": "9h"}
```

Função a SUBSTITUIR (não manter):
```python
def _selecionar_slots_dias_diferentes(slots: list[dict]) -> list[dict]:
    # Seleciona até 3 slots garantindo dias diferentes (sem considerar preferência)
    # SUBSTITUIR por _priorizar_slots()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Algoritmo _priorizar_slots — preferência + dias diferentes</name>
  <files>app/agents/retencao.py, tests/test_retencao.py</files>
  <behavior>
    - Test 1: _priorizar_slots(pool, dia_preferido=0, hora_preferida=9) — pool tem segunda às 9h → opção 1 é essa segunda às 9h; opções 2 e 3 são próximos disponíveis em dias DIFERENTES da opção 1
    - Test 2: _priorizar_slots(pool, dia_preferido=0, hora_preferida=None) — sem hora → opção 1 é qualquer slot de segunda; opções 2 e 3 em outros dias
    - Test 3: _priorizar_slots(pool, dia_preferido=0, hora_preferida=9) — sem slot de segunda → sem mencionar preferência, retorna 3 primeiros disponíveis em dias diferentes
    - Test 4: _priorizar_slots(pool=[], dia_preferido=None, hora_preferida=None) — pool vazio → retorna []
    - Test 5: _priorizar_slots — pool com 5 slots todos na terça → retorna até 3 slots da terça (D-13 é "preferência", não regra dura; mas tenta variar dias)
    - Test 6: _priorizar_slots — pool com slots em 4 dias diferentes → retorna exatamente 3 slots, cada um em dia diferente
    - Test 7: _priorizar_slots — retorna no máximo 3 slots independente do tamanho do pool
  </behavior>
  <action>
    Substituir _selecionar_slots_dias_diferentes() por _priorizar_slots() em app/agents/retencao.py:

    ```python
    def _priorizar_slots(
        pool: list[dict],
        dia_preferido: int | None,
        hora_preferida: int | None,
    ) -> list[dict]:
        """
        Seleciona até 3 slots com priorização (per D-09 a D-13):
          - Opção 1: slot que melhor corresponde à preferência (dia + hora)
          - Opções 2 e 3: próximos disponíveis em dias diferentes
          - Se sem preferência: 3 primeiros em dias diferentes
          - D-13: slots em dias diferentes são preferidos — nunca 3 no mesmo dia se houver alternativa
        """
    ```

    Algoritmo interno:
    1. Se dia_preferido is not None:
       a. Filtra slots com weekday() == dia_preferido → slots_dia_pref
       b. Se slots_dia_pref e hora_preferida is not None:
          - Dentro de slots_dia_pref, filtra _hora_int(s) == hora_preferida → slot_exato
          - slot_escolhido = slot_exato[0] se existir, senão slots_dia_pref[0]
       c. Senão (só dia preferido): slot_escolhido = slots_dia_pref[0] se existir
       d. Se slot_escolhido: selecionados = [slot_escolhido]
          Preencher restantes (até 3): percorrer pool, pular slots no mesmo dia que selecionados,
          pular o slot_escolhido já adicionado, adicionar até completar 3
          Se não houver 3 em dias diferentes, completar com qualquer slot restante
       e. Se NÃO encontrou slot na preferência: usar modo sem preferência (próximo passo)
    2. Modo sem preferência:
       - Percorrer pool em ordem
       - Selecionar no máximo 3, priorizando dias diferentes (set de dias usados)
       - Se não houver 3 dias diferentes, completar com próximos slots mesmo que repitam dia
    3. Retornar lista de até 3 slots

    Atualizar todas as chamadas a _selecionar_slots_dias_diferentes() para _priorizar_slots().

    No código de _fluxo_remarcacao etapa "coletando_preferencia", passar dia_preferido e hora_preferida
    (que já são parseados nessa etapa) para _priorizar_slots(self._slots_pool, dia_preferido, hora_preferida).
    Também simplificar o aviso de "preferência não disponível":
    - Se dia_preferido is not None e não há slot naquele dia: exibir aviso per D-12
      "Não tenho {dia}-feira disponível na janela, mas veja o que temos:"
    - Caso contrário (sem aviso ou preferência encontrada): mostrar diretamente MSG_OPCOES_REMARCACAO

    Adicionar os novos testes em tests/test_retencao.py.
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_retencao.py -q -k "priorizar" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - _priorizar_slots exportada e testada com 7 casos
    - _selecionar_slots_dias_diferentes removida ou marcada como deprecated (não chamada)
    - Algoritmo sempre retorna exatamente 3 slots quando pool tem >= 3 elementos
    - Preferência respeitada: primeiro slot é da preferência quando disponível
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fluxo de negociação — 2 rodadas + perda de retorno + mensagens fixas</name>
  <files>app/agents/retencao.py, tests/test_retencao.py</files>
  <behavior>
    - Test 1: paciente rejeita 3 slots (rodada_negociacao=0) → Ana oferece mais 3 do pool restante (rodada_negociacao vira 1)
    - Test 2: paciente rejeita segunda rodada (rodada_negociacao=1) → Ana declara perda de retorno (etapa vira "perda_retorno")
    - Test 3: na etapa "perda_retorno" → Ana informa que o prazo não comporta remarcação e oferece agendar como nova consulta
    - Test 4: pool com só 3 slots — paciente rejeita → Ana detecta que não há mais slots no pool e declara perda diretamente
    - Test 5: paciente escolhe slot válido na rodada 2 → fluxo confirma normalmente (sem declarar perda)
    - Test 6: MSG_INICIO_REMARCACAO contém "7 dias" e "prazo máximo" — per D-08 não cita data exata
  </behavior>
  <action>
    Modificar a etapa "oferecendo_slots" em _fluxo_remarcacao para implementar as 2 rodadas de negociação:

    1. Adicionar novas constantes de mensagem no topo de retencao.py (per D-26/D-27):
       ```python
       MSG_SEGUNDA_RODADA = (
           "Entendo 😊 Vou buscar mais opções pra você:\n\n"
           "{opcoes}\n\n"
           "Alguma dessas funciona?"
       )

       MSG_PERDA_RETORNO = (
           "Infelizmente não conseguimos encontrar um horário que funcione para você "
           "dentro do prazo de remarcação 😔\n\n"
           "Como o prazo se encerra em breve, o retorno não poderá mais ser remarcado.\n\n"
           "Mas posso te ajudar a agendar uma consulta nova! Quer que eu verifique os "
           "planos disponíveis para você? 💚"
       )

       MSG_SEM_MAIS_SLOTS = (
           "Não há mais horários disponíveis na janela de remarcação 😔\n\n"
           "Como o prazo se encerra em breve, o retorno não poderá mais ser remarcado.\n\n"
           "Mas posso te ajudar a agendar uma consulta nova! Quer que eu verifique os "
           "planos disponíveis para você? 💚"
       )
       ```

    2. Reestruturar a lógica da etapa "oferecendo_slots":
       a. Tentar extrair escolha do paciente com _extrair_escolha_slot(msg, self._slots_oferecidos)
       b. Se escolha encontrada → ir para etapa "confirmar_slot" (nova etapa intermediária antes de "concluido")
          A confirmação Dietbox vem no Plan 02-03; por ora avanço para "aguardando_confirmacao_dietbox"
       c. Se NÃO escolha (rejeição):
          - Calcular next_batch: slots do self._slots_pool que não estão em self._slots_oferecidos
            (usar set de datetimes para comparar)
          - Se next_batch vazio OU self.rodada_negociacao >= 1:
            → self.etapa = "perda_retorno"
            → retornar MSG_PERDA_RETORNO
          - Se next_batch disponível E self.rodada_negociacao == 0:
            → self.rodada_negociacao += 1
            → self._slots_oferecidos = _priorizar_slots(next_batch, None, None)  # sem preferência na 2a rodada
            → opcoes = "\n".join(f"{i+1}. {s['data_fmt']} às {s['hora']}" for i, s in enumerate(self._slots_oferecidos))
            → retornar MSG_SEGUNDA_RODADA.format(opcoes=opcoes)

    3. Adicionar etapa "perda_retorno" em _fluxo_remarcacao:
       if self.etapa == "perda_retorno":
           # Qualquer mensagem nesta etapa → oferecer nova consulta (redirecionar para atendimento)
           self.etapa = "redirecionando_atendimento"
           return ["Claro! Vou te encaminhar para o fluxo de agendamento normal 💚"]

    4. Garantir que MSG_INICIO_REMARCACAO (per D-08) NÃO cita data exata:
       A mensagem atual já contém "dentro dos próximos 7 dias, que é o prazo máximo" — confirmar
       que esta frase permanece, não foi substituída por data calculada (self.fim_janela não aparece na msg)

    Adicionar os novos testes em tests/test_retencao.py.
    Os testes devem usar um AgenteRetencao com _slots_pool pré-preenchido para não precisar de mock Dietbox.
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_retencao.py -q -k "negociacao or perda_retorno or rodada" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Rejeição na primeira rodada → segunda rodada com novos 3 slots
    - Rejeição na segunda rodada → etapa "perda_retorno" com MSG_PERDA_RETORNO
    - Pool sem mais slots → MSG_SEM_MAIS_SLOTS (ou MSG_PERDA_RETORNO direto)
    - MSG_INICIO_REMARCACAO não cita data calculada internamente
    - python -m pytest tests/ -q sem regressões
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paciente → FSM | Paciente pode enviar qualquer texto como "escolha" de slot — parser deve ser robusto |
| _slots_pool (Redis) → negociação | Pool deserializado do Redis deve ser confiável para calcular next_batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | _slots_pool deserializado do Redis com slots inválidos | mitigate | Validar que cada slot tem chave "datetime" antes de usar; retornar [] se inválido |
| T-02-02-02 | Denial of Service | Loop infinito de negociação se rodada_negociacao não incrementar | mitigate | Condição explícita: rodada_negociacao >= 1 → perda_retorno (não >= próxima rodada) |
| T-02-02-03 | Spoofing | Paciente injeta texto que parece escolha de slot ("1" ou data válida) mas na etapa errada | accept | _extrair_escolha_slot só é chamado na etapa correta; baixo risco de abuso |
| T-02-02-04 | Information Disclosure | Mensagem de "perda de retorno" revela prazo interno calculado | mitigate | MSG_PERDA_RETORNO não cita data calculada (per D-08 aplicado por analogia) |
</threat_model>

<verification>
Após execução:

1. python -m pytest tests/test_retencao.py -q — todos verde incluindo testes de 02-01
2. python -m pytest tests/ -q — zero regressões
3. Verificar manualmente:
   - Criar AgenteRetencao, preencher _slots_pool com 6 slots em dias diferentes
   - Simular rejeição → deve entrar em rodada 2
   - Simular segunda rejeição → deve declarar perda_retorno
</verification>

<success_criteria>
- _priorizar_slots implementada e testada: 7 casos incluindo pool vazio, preferência encontrada, preferência ausente
- Negociação em 2 rodadas funcionando: pool dividido em batches de 3, rodada_negociacao incrementado corretamente
- Perda de retorno declarada apenas após 2 rodadas (ou pool esgotado)
- Mensagens per D-26/D-27: informal, acolhedor, emojis moderados
- Zero regressões em tests/
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-fluxo-de-remarca-o/02-02-SUMMARY.md`
</output>
