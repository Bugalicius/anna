---
phase: 02-fluxo-de-remarca-o
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/agents/dietbox_worker.py
  - app/agents/retencao.py
  - tests/test_dietbox_worker.py
  - tests/test_retencao.py
autonomous: true
requirements:
  - REMC-04
must_haves:
  truths:
    - "O horário é alterado no Dietbox ANTES de Ana enviar qualquer mensagem de confirmação ao paciente"
    - "A observação 'Remarcado do dia X para dia Y' é adicionada ao agendamento no Dietbox"
    - "Se o write no Dietbox falhar, Ana informa o paciente do problema sem enviar confirmação falsa"
    - "Se o write no Dietbox for bem-sucedido, Ana envia a confirmação com data, hora e modalidade corretas"
  artifacts:
    - path: "app/agents/dietbox_worker.py"
      provides: "alterar_agendamento() — altera data/hora de agendamento existente via PATCH e adiciona observação"
      exports: ["alterar_agendamento"]
    - path: "app/agents/retencao.py"
      provides: "etapa aguardando_confirmacao_dietbox que chama alterar_agendamento() antes de retornar confirmação ao paciente"
      contains: "aguardando_confirmacao_dietbox, MSG_ERRO_REMARCACAO_DIETBOX"
    - path: "tests/test_dietbox_worker.py"
      provides: "Testes para alterar_agendamento() cobrindo sucesso, falha HTTP e timeout"
    - path: "tests/test_retencao.py"
      provides: "Testes para o fluxo aguardando_confirmacao_dietbox: sucesso Dietbox, falha Dietbox"
  key_links:
    - from: "app/agents/retencao.py:etapa oferecendo_slots — slot escolhido"
      to: "app/agents/retencao.py:etapa aguardando_confirmacao_dietbox"
      via: "ao escolher slot, etapa muda para aguardando_confirmacao_dietbox (não para concluido)"
      pattern: "aguardando_confirmacao_dietbox"
    - from: "app/agents/retencao.py:aguardando_confirmacao_dietbox"
      to: "app/agents/dietbox_worker.py:alterar_agendamento"
      via: "chamada com id_agenda_original, novo dt_inicio e observacao"
      pattern: "alterar_agendamento"
    - from: "app/agents/dietbox_worker.py:alterar_agendamento"
      to: "DIETBOX_API/agenda/{id_agenda}"
      via: "PATCH request com Start, End e Observacao"
      pattern: "requests\\.patch.*agenda"
---

<objective>
Implementar a sequência obrigatória: Dietbox write → verificação de sucesso → confirmação ao paciente.

Purpose: O código atual (etapa "oferecendo_slots" em retencao.py linha 321) envia MSG_CONFIRMACAO_REMARCACAO imediatamente após o paciente escolher um slot — sem tocar no Dietbox. Isso viola D-20/D-21 e significa que a nutri vê a agenda desatualizada enquanto o paciente acredita que remarcou. Este plano fecha essa lacuna: o slot é primeiro alterado no Dietbox, o sucesso é verificado, e apenas então a confirmação é enviada.

Output:
- alterar_agendamento(id_agenda, novo_dt_inicio, observacao) em dietbox_worker.py
- Etapa "aguardando_confirmacao_dietbox" em retencao.py chamada quando paciente escolhe slot
- Etapa "oferecendo_slots" modificada: ao extrair escolha, muda etapa para "aguardando_confirmacao_dietbox" (não "concluido")
- Mensagem de erro informativa quando Dietbox falha (per D-21: nunca confirmar sem sucesso Dietbox)
- Testes cobrindo sucesso, falha HTTP e fallback de erro
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fluxo-de-remarca-o/02-CONTEXT.md
@docs/regras_remarcacao.md
@app/agents/dietbox_worker.py
@app/agents/retencao.py
@tests/test_dietbox_worker.py
@tests/test_retencao.py

<interfaces>
<!-- Contratos produzidos pelos planos 02-01 e 02-02 que este plano consome -->

De app/agents/dietbox_worker.py (existente, padrão a seguir):
```python
DIETBOX_API = "https://api.dietbox.me/v2"

def _headers() -> dict:
    """Retorna headers com Bearer token."""

def agendar_consulta(
    id_paciente: int,
    dt_inicio: datetime,
    modalidade: str,
    plano: str,
    duracao_minutos: int = 60,
) -> str:
    """Agenda consulta via POST /agenda. Retorna id_agenda (str UUID). Levanta ValueError se sem ID."""

# Padrão de request: requests.patch/post com _headers(), timeout=20, resp.raise_for_status()
```

De app/agents/retencao.py (após planos 02-01 e 02-02):
```python
class AgenteRetencao:
    self.id_agenda_original: str | None  # preenchido por _detectar_tipo_remarcacao em 02-01
    self.novo_slot: dict | None          # slot escolhido pelo paciente; formato: {"datetime": "...", "data_fmt": "...", "hora": "..."}
    self.etapa: str                      # "oferecendo_slots" → deve mudar para "aguardando_confirmacao_dietbox"

# Etapa atual (código a ser MODIFICADO em retencao.py linhas 318-331):
if self.etapa == "oferecendo_slots":
    slot = _extrair_escolha_slot(msg, self._slots_oferecidos)
    if slot:
        self.etapa = "concluido"           # ← ERRADO: não chama Dietbox
        return [MSG_CONFIRMACAO_REMARCACAO.format(...)]
    ...

# Formato de slot (de consultar_slots_disponiveis):
# {"datetime": "2026-04-14T09:00:00", "data_fmt": "segunda, 14/04", "hora": "9h"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: alterar_agendamento() em dietbox_worker.py — PATCH com nova data e observação</name>
  <files>app/agents/dietbox_worker.py, tests/test_dietbox_worker.py</files>
  <read_first>
    - app/agents/dietbox_worker.py — ler por completo antes de editar; ver padrão de agendar_consulta() (linhas 425-480) para usar a mesma estrutura de request
    - tests/test_dietbox_worker.py — ler testes existentes para seguir o mesmo padrão com patch("requests.get") / patch("requests.post")
  </read_first>
  <behavior>
    - Test 1: alterar_agendamento("ID-123", datetime(2026,4,14,9,0), "Remarcado do dia 10/04 para 14/04") — mock requests.patch retorna status 200 com JSON {"Data":{"id":"ID-123"}} → retorna True
    - Test 2: alterar_agendamento quando requests.patch levanta requests.HTTPError (status 400) → retorna False, nunca propaga exceção
    - Test 3: alterar_agendamento quando requests.patch levanta requests.Timeout → retorna False com logger.error chamado
    - Test 4: alterar_agendamento — o payload enviado ao mock contém as chaves "Start", "End", "Observacao" com os valores corretos (verificar mock call_args)
    - Test 5: alterar_agendamento — URL da chamada é f"{DIETBOX_API}/agenda/{id_agenda}" (verificar mock call_args[0][0])
  </behavior>
  <action>
    Adicionar a função alterar_agendamento() em app/agents/dietbox_worker.py, logo após agendar_consulta() (após a linha que encerra a função, antes de lancar_financeiro):

    ```python
    def alterar_agendamento(
        id_agenda: str,
        novo_dt_inicio: datetime,
        observacao: str,
        duracao_minutos: int = 60,
    ) -> bool:
        """
        Altera a data/hora de um agendamento existente no Dietbox (per D-22).
        Adiciona observacao ao campo Observacao do agendamento (per D-23).
        Retorna True se bem-sucedido, False em qualquer falha (nunca propaga exceção).
        """
        if novo_dt_inicio.tzinfo is None:
            novo_dt_inicio = novo_dt_inicio.replace(tzinfo=BRT)
        novo_dt_fim = novo_dt_inicio + timedelta(minutes=duracao_minutos)

        payload = {
            "Start": novo_dt_inicio.isoformat(),
            "End": novo_dt_fim.isoformat(),
            "Observacao": observacao,
        }
        try:
            resp = requests.patch(
                f"{DIETBOX_API}/agenda/{id_agenda}",
                headers=_headers(),
                json=payload,
                timeout=20,
            )
            resp.raise_for_status()
            logger.info(
                "Agendamento alterado: id=%s, novo_inicio=%s",
                id_agenda,
                novo_dt_inicio.isoformat(),
            )
            return True
        except Exception as e:
            logger.error("Falha ao alterar agendamento %s: %s", id_agenda, e)
            return False
    ```

    Adicionar "alterar_agendamento" às importações em retencao.py (a ser feito na Task 2).

    Escrever os 5 testes em tests/test_dietbox_worker.py usando unittest.mock.patch("requests.patch") e MagicMock. Seguir o mesmo padrão dos testes existentes nesse arquivo: usar @patch como decorator ou context manager, configurar mock.return_value.status_code e mock.return_value.json.return_value.
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_dietbox_worker.py -q -k "alterar_agendamento" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - alterar_agendamento() adicionada em dietbox_worker.py após agendar_consulta()
    - 5 testes passando sem chamadas HTTP reais
    - Retorna bool (True/False), nunca propaga exceção
    - Payload inclui "Start", "End", "Observacao"
    - URL usada é DIETBOX_API + "/agenda/" + id_agenda com PATCH
    - python -m pytest tests/test_dietbox_worker.py -q retorna verde sem regressões
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sequência Dietbox-first em retencao.py — etapa aguardando_confirmacao_dietbox</name>
  <files>app/agents/retencao.py, tests/test_retencao.py</files>
  <read_first>
    - app/agents/retencao.py — ler arquivo completo antes de editar; focar em:
      * Bloco "Etapa 3: paciente escolheu um slot" (linhas 317-331) — é aqui que a mudança ocorre
      * Método to_dict() e from_dict() — para adicionar o novo campo "novo_slot_dt_iso"
      * Importações no topo — para adicionar "alterar_agendamento"
    - tests/test_retencao.py — ler testes existentes para seguir o mesmo padrão de mock
  </read_first>
  <behavior>
    - Test 1: paciente escolhe slot válido com id_agenda_original="ID-999" → etapa muda para "aguardando_confirmacao_dietbox", self.novo_slot armazena o slot escolhido, retorna ["Um instante, por favor 💚"] (indicador de espera enquanto chama Dietbox)
    - Test 2: na etapa "aguardando_confirmacao_dietbox" com mock alterar_agendamento retornando True → etapa muda para "concluido", retorna [MSG_CONFIRMACAO_REMARCACAO] com data/hora corretos do slot
    - Test 3: na etapa "aguardando_confirmacao_dietbox" com mock alterar_agendamento retornando False → etapa muda para "erro_remarcacao", retorna [MSG_ERRO_REMARCACAO_DIETBOX] (sem confirmação falsa ao paciente)
    - Test 4: na etapa "aguardando_confirmacao_dietbox" com id_agenda_original=None → chama alterar_agendamento com id_agenda="" OU trata como erro imediato e retorna MSG_ERRO_REMARCACAO_DIETBOX (implementar o que fizer mais sentido, documentar no código)
    - Test 5: MSG_CONFIRMACAO_REMARCACAO no retorno da Test 2 contém a data_fmt e hora do slot escolhido (não valores hardcoded)
    - Test 6: na etapa "erro_remarcacao" com qualquer mensagem do paciente → retorna orientação para tentar remarcar novamente ou entrar em contato com a Thaynara diretamente
  </behavior>
  <action>
    Modificar app/agents/retencao.py:

    1. Adicionar "alterar_agendamento" ao import de dietbox_worker no topo do arquivo:
       DE:  from app.agents.dietbox_worker import consultar_slots_disponiveis
       PARA: from app.agents.dietbox_worker import (
                 consultar_slots_disponiveis,
                 alterar_agendamento,
             )
       (após planos 02-01/02-02, este import pode já ter outros nomes — adicionar alterar_agendamento
        sem remover os existentes)

    2. Adicionar nova constante de mensagem (per D-26/D-27) após MSG_CONFIRMACAO_REMARCACAO:
       ```python
       MSG_ERRO_REMARCACAO_DIETBOX = (
           "Ops! Tive um problema técnico ao tentar confirmar a remarcação no sistema 😔\n\n"
           "Vou acionar a Thaynara para resolver isso manualmente. "
           "Você receberá uma confirmação assim que estiver tudo certo 💚"
       )

       MSG_ERRO_REMARCACAO_RETRY = (
           "Ainda estou com dificuldade técnica para confirmar no sistema 😔\n\n"
           "Por favor, entre em contato diretamente com a Thaynara para garantir sua remarcação. "
           "Me desculpe o inconveniente! 💚"
       )
       ```

    3. Modificar o bloco "Etapa 3: paciente escolheu um slot" (etapa "oferecendo_slots"):
       ANTES (código atual):
       ```python
       if self.etapa == "oferecendo_slots":
           slot = _extrair_escolha_slot(msg, self._slots_oferecidos)
           if slot:
               self.etapa = "concluido"
               return [MSG_CONFIRMACAO_REMARCACAO.format(
                   data=slot["data_fmt"],
                   hora=slot["hora"],
                   modalidade=self.modalidade,
               )]
       ```
       DEPOIS (substituir pelo bloco abaixo):
       ```python
       if self.etapa == "oferecendo_slots":
           slot = _extrair_escolha_slot(msg, self._slots_oferecidos)
           if slot:
               self.novo_slot = slot
               self.etapa = "aguardando_confirmacao_dietbox"
               # Indicador de espera antes de chamar Dietbox (per comportamento estabelecido na Fase 1)
               return ["Um instante, por favor 💚"]
       ```
       ATENÇÃO: o bloco de rejeição (opcoes repetidas) ABAIXO do if slot deve ser preservado intacto.
       A lógica de negociação (2 rodadas + perda_retorno) adicionada em 02-02 também deve ser preservada.

    4. Adicionar etapa "aguardando_confirmacao_dietbox" em _fluxo_remarcacao, após o bloco "oferecendo_slots":
       ```python
       # Etapa 4: chamar Dietbox para alterar agendamento e confirmar ao paciente
       if self.etapa == "aguardando_confirmacao_dietbox":
           if not self.novo_slot:
               # Estado inconsistente — não deveria acontecer
               logger.error("aguardando_confirmacao_dietbox sem novo_slot: telefone=%s", self.telefone[-4:])
               self.etapa = "erro_remarcacao"
               return [MSG_ERRO_REMARCACAO_DIETBOX]

           from datetime import datetime as _dt
           novo_dt = _dt.fromisoformat(self.novo_slot["datetime"])

           # Monta observação per D-23
           data_original = ""
           if self.consulta_atual:
               inicio_original = self.consulta_atual.get("inicio", "")
               if inicio_original:
                   try:
                       dt_orig = _dt.fromisoformat(inicio_original)
                       data_original = dt_orig.strftime("%d/%m/%Y")
                   except ValueError:
                       data_original = inicio_original[:10]
           data_nova = novo_dt.strftime("%d/%m/%Y")
           observacao = f"Remarcado do dia {data_original} para {data_nova}" if data_original else f"Remarcado para {data_nova}"

           id_agenda = self.id_agenda_original or ""
           sucesso = alterar_agendamento(id_agenda, novo_dt, observacao)

           if sucesso:
               self.etapa = "concluido"
               return [MSG_CONFIRMACAO_REMARCACAO.format(
                   data=self.novo_slot["data_fmt"],
                   hora=self.novo_slot["hora"],
                   modalidade=self.modalidade,
               )]
           else:
               self.etapa = "erro_remarcacao"
               return [MSG_ERRO_REMARCACAO_DIETBOX]

       # Etapa 5: erro técnico — orientar paciente
       if self.etapa == "erro_remarcacao":
           return [MSG_ERRO_REMARCACAO_RETRY]
       ```

    5. Garantir que to_dict() inclui "novo_slot" (confirmar que já existia — se não existia, adicionar):
       Adicionar em to_dict() se ausente:
           "novo_slot": self.novo_slot,
       E em from_dict() se ausente:
           agent.novo_slot = data.get("novo_slot")

    6. Escrever os 6 testes em tests/test_retencao.py usando unittest.mock.patch(
       "app.agents.retencao.alterar_agendamento") para mockar a chamada ao Dietbox.
       Criar instância AgenteRetencao pré-configurada com:
         - etapa="oferecendo_slots" (para Test 1)
         - etapa="aguardando_confirmacao_dietbox" + novo_slot preenchido (para Tests 2-5)
         - etapa="erro_remarcacao" (para Test 6)
       Não precisa de mock do Redis nem do Dietbox de leitura neste conjunto de testes.
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_retencao.py -q -k "dietbox or confirmacao or aguardando" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Etapa "aguardando_confirmacao_dietbox" existe em _fluxo_remarcacao
    - Etapa "oferecendo_slots" muda para "aguardando_confirmacao_dietbox" (não "concluido") ao extrair slot
    - alterar_agendamento() é chamado ANTES de retornar MSG_CONFIRMACAO_REMARCACAO
    - Falha no Dietbox retorna MSG_ERRO_REMARCACAO_DIETBOX e muda etapa para "erro_remarcacao" (sem confirmação falsa)
    - Observação inclui "Remarcado do dia X para Y" per D-23
    - 6 testes passando
    - python -m pytest tests/ -q retorna zero regressões
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retencao.py → Dietbox API | Write de alteração de agendamento; falha não deve resultar em confirmação falsa ao paciente |
| paciente → etapa FSM | Paciente não deve receber confirmação se Dietbox não confirmou — o FSM garante isso |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | alterar_agendamento com id_agenda vazio ou None | mitigate | Se id_agenda_original for None, logar erro e retornar MSG_ERRO_REMARCACAO_DIETBOX sem chamar Dietbox; test 4 cobre isso |
| T-02-03-02 | Denial of Service | Timeout no PATCH do Dietbox bloqueia resposta ao paciente | mitigate | timeout=20 em requests.patch + try/except retorna False imediatamente; paciente vê MSG_ERRO em vez de travar |
| T-02-03-03 | Spoofing | Paciente adultera mensagem para pular etapa aguardando_confirmacao_dietbox | accept | Etapa é controlada pelo FSM (não pelo conteúdo da mensagem do paciente); etapa só é ativada por código interno |
| T-02-03-04 | Information Disclosure | Observação "Remarcado do dia X para Y" expõe data original da consulta | accept | Dado va apenas para o Dietbox (sistema da nutri); nunca aparece na mensagem ao paciente — baixo risco |
| T-02-03-05 | Elevation of Privilege | alterar_agendamento chamado com id_agenda de outro paciente | accept | id_agenda_original vem do Dietbox (consultar_agendamento_ativo em 02-01), não do paciente; Dietbox valida o token da nutri |
</threat_model>

<verification>
Após execução:

1. python -m pytest tests/test_dietbox_worker.py -q -k "alterar_agendamento" — 5 testes verde
2. python -m pytest tests/test_retencao.py -q -k "dietbox or confirmacao or aguardando" — 6 testes verde
3. python -m pytest tests/ -q — zero regressões em toda a suite
4. Verificar manualmente a sequência lógica:
   - Grep em retencao.py: grep "aguardando_confirmacao_dietbox" app/agents/retencao.py — deve aparecer em pelo menos 2 lugares (atribuição e condicional)
   - Grep em retencao.py: grep "alterar_agendamento" app/agents/retencao.py — deve aparecer no corpo de _fluxo_remarcacao
   - Grep em retencao.py: confirmar que "concluido" NÃO aparece mais na etapa "oferecendo_slots" (paciente escolhe slot) — o único caminho para "concluido" é via "aguardando_confirmacao_dietbox" com sucesso Dietbox
</verification>

<success_criteria>
- alterar_agendamento() em dietbox_worker.py: PATCH para /agenda/{id_agenda} com Start, End, Observacao; retorna bool; nunca propaga exceção
- Etapa "aguardando_confirmacao_dietbox" em retencao.py: chama alterar_agendamento antes de enviar confirmação ao paciente
- Sucesso Dietbox → MSG_CONFIRMACAO_REMARCACAO com dados corretos do slot
- Falha Dietbox → MSG_ERRO_REMARCACAO_DIETBOX, etapa "erro_remarcacao", zero confirmação falsa
- Observação per D-23: "Remarcado do dia X para Y" (data_original pode ser "" se consulta_atual ausente)
- 11 novos testes (5 + 6) passando
- Zero regressões em tests/
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-fluxo-de-remarca-o/02-03-SUMMARY.md`
</output>
