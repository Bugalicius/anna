---
phase: 02-fluxo-de-remarca-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/agents/dietbox_worker.py
  - app/agents/retencao.py
  - tests/test_dietbox_worker.py
  - tests/test_retencao.py
autonomous: true
requirements:
  - REMC-01
  - REMC-03
must_haves:
  truths:
    - "Ana distingue remarcação de retorno (consulta + lançamento financeiro) de nova consulta (sem lançamento)"
    - "Ana comunica 'dentro dos próximos 7 dias' mas calcula a janela a partir da semana do agendamento original, não de hoje"
    - "Slots buscados começam em amanhã (nunca no dia atual) e são apenas seg-sex"
    - "Paciente sem consulta agendada ou sem lançamento financeiro é encaminhado para fluxo de nova consulta"
  artifacts:
    - path: "app/agents/dietbox_worker.py"
      provides: "consultar_agendamento_ativo() e verificar_lancamento_financeiro()"
      exports: ["consultar_agendamento_ativo", "verificar_lancamento_financeiro"]
    - path: "app/agents/retencao.py"
      provides: "FSM expandido: novos campos de estado + detecção retorno/nova consulta + cálculo de janela correto"
      contains: "tipo_remarcacao, id_agenda_original, _slots_pool, rodada_negociacao"
    - path: "tests/test_dietbox_worker.py"
      provides: "Testes para as novas funções Dietbox"
    - path: "tests/test_retencao.py"
      provides: "Testes para detecção de retorno e cálculo de janela"
  key_links:
    - from: "app/agents/retencao.py:_fluxo_remarcacao"
      to: "app/agents/dietbox_worker.py:consultar_agendamento_ativo"
      via: "chamada síncrona no início do fluxo"
      pattern: "consultar_agendamento_ativo"
    - from: "app/agents/retencao.py"
      to: "app/agents/dietbox_worker.py:verificar_lancamento_financeiro"
      via: "checagem após obter id_agenda"
      pattern: "verificar_lancamento_financeiro"
    - from: "app/agents/retencao.py:AgenteRetencao.from_dict"
      to: "app/agents/retencao.py:AgenteRetencao"
      via: "data.get(campo, default) para todos os novos campos"
      pattern: "\\.get\\(.*rodada_negociacao"
---

<objective>
Implementar a detecção de retorno vs. nova consulta e o cálculo correto da janela de remarcação.

Purpose: A implementação atual em retencao.py calcula a janela a partir de hoje (errado) e não verifica se o paciente já pagou. Estas duas falhas fazem a Ana aplicar as regras erradas para todos os pacientes de retorno — o tipo de remarcação mais frequente.

Output:
- Duas novas funções em dietbox_worker.py: consultar_agendamento_ativo() e verificar_lancamento_financeiro()
- AgenteRetencao expandido com tipo_remarcacao, id_agenda_original, _slots_pool, rodada_negociacao
- from_dict() seguro com .get(campo, default) para todos os novos campos
- Janela calculada a partir da semana do agendamento original (sexta da semana seguinte)
- Início da busca: amanhã, nunca o mesmo dia
- Testes cobrindo os novos comportamentos
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fluxo-de-remarca-o/02-CONTEXT.md
@.planning/phases/02-fluxo-de-remarca-o/02-RESEARCH.md
@docs/regras_remarcacao.md
@app/agents/dietbox_worker.py
@app/agents/retencao.py
@app/state_manager.py
@tests/test_dietbox_worker.py

<interfaces>
<!-- Contratos existentes que este plano consome e expande -->

De app/agents/dietbox_worker.py:
```python
DIETBOX_API = "https://api.dietbox.me/v2"

def _headers() -> dict:
    """Retorna headers com Bearer token."""

def consultar_slots_disponiveis(
    modalidade: str = "presencial",
    dias_a_frente: int = 21,
    data_inicio: "date | None" = None,
) -> list[dict]:
    """
    Cada slot: {"datetime": "2026-04-10T09:00:00", "data_fmt": "sexta, 10/04", "hora": "9h"}
    """
```

De app/agents/retencao.py (estado atual que SERÁ EXPANDIDO):
```python
class AgenteRetencao:
    def __init__(self, telefone: str, nome: str | None, modalidade: str = "presencial") -> None:
        self.etapa: str = "inicio"
        self.consulta_atual: dict | None = None
        self.novo_slot: dict | None = None
        self._slots_oferecidos: list[dict] = []
        self._preferencia_horario: str = ""

    def to_dict(self) -> dict:
        # Campos atuais: _tipo, telefone, nome, modalidade, etapa, motivo, consulta_atual, novo_slot, historico

    @classmethod
    def from_dict(cls, data: dict) -> "AgenteRetencao":
        # from_dict atual — DEVE ser expandido com .get(campo, default) para novos campos
```

De app/state_manager.py:
```python
class RedisStateManager:
    async def load(self, phone_hash: str) -> object | None:
        # Chama AgenteRetencao.from_dict(data) quando _tipo == "retencao"
    async def save(self, phone_hash: str, agent: object) -> None:
        # Chama agent.to_dict()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Funções Dietbox — consultar_agendamento_ativo e verificar_lancamento_financeiro</name>
  <files>app/agents/dietbox_worker.py, tests/test_dietbox_worker.py</files>
  <behavior>
    - Test 1: consultar_agendamento_ativo(id_paciente=123) — Dietbox retorna lista com 1 agenda futura → retorna dict com {id, inicio, fim, id_servico, desmarcada}
    - Test 2: consultar_agendamento_ativo quando lista vazia → retorna None
    - Test 3: consultar_agendamento_ativo quando todas as agendas têm desmarcada=True → retorna None
    - Test 4: verificar_lancamento_financeiro(id_agenda="ABC") — Dietbox retorna lançamento com pago=True → retorna True
    - Test 5: verificar_lancamento_financeiro quando lista vazia → retorna False
    - Test 6: verificar_lancamento_financeiro quando exceção HTTP → retorna False (nunca deixa subir exceção)
  </behavior>
  <action>
    Adicionar duas funções novas em app/agents/dietbox_worker.py, após a seção "Agendamento":

    1. consultar_agendamento_ativo(id_paciente: int) -> dict | None
       - GET {DIETBOX_API}/agenda?IdPaciente={id_paciente}&Start={hoje}T00:00:00&End={hoje+180d}T23:59:59
       - Filtra itens onde desmarcada != True e inicio >= agora
       - Ordena por inicio ASC, retorna o primeiro
       - Retorna dict com chaves: id (str), inicio (str ISO), fim (str ISO), id_servico (str|None)
       - Retorna None se lista vazia ou erro
       - Wrap em try/except, logger.error em caso de falha

    2. verificar_lancamento_financeiro(id_agenda: str) -> bool
       - GET {DIETBOX_API}/financeiro?IdAgenda={id_agenda} (ou endpoint equivalente, inferir do padrão REST do Dietbox)
       - Se a resposta contiver qualquer item na lista → retorna True (existe lançamento = pagou)
       - Se lista vazia → retorna False (sem lançamento = não pagou ainda)
       - Wrap em try/except com logger.error, retorna False em caso de erro
       - Nunca propaga exceção para o chamador

    Padrão de request: usar requests.get com _headers() e timeout=15, como o restante do arquivo.

    Escrever testes em tests/test_dietbox_worker.py usando patch("requests.get") e MagicMock — mesmo padrão dos testes existentes nesse arquivo.
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_dietbox_worker.py -q -k "agendamento_ativo or lancamento_financeiro" 2>&1 | tail -20</automated>
  </verify>
  <done>
    - consultar_agendamento_ativo e verificar_lancamento_financeiro exportadas em dietbox_worker.py
    - 6 testes passando sem chamadas HTTP reais
    - Nenhum teste existente quebrado: python -m pytest tests/test_dietbox_worker.py -q retorna verde
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgenteRetencao — expansão de estado, detecção retorno/nova consulta e cálculo de janela correto</name>
  <files>app/agents/retencao.py, tests/test_retencao.py</files>
  <behavior>
    - Test 1: calcular_fim_janela(data_consulta=date(2026,4,14)) — consulta na semana 13-17/abr → retorna date(2026,4,24) (sexta da semana seguinte)
    - Test 2: calcular_fim_janela(data_consulta=date(2026,4,17)) — consulta na sexta 17/abr → retorna date(2026,4,24) (mesma sexta da semana seguinte)
    - Test 3: calcular_fim_janela(data_consulta=date(2026,4,20)) — consulta segunda 20/abr → retorna date(2026,4,1 de maio, uma sexta) — sexta da semana 20-24/abr
    - Test 4: AgenteRetencao.from_dict com dict SEM rodada_negociacao → rodada_negociacao = 0 (não quebra)
    - Test 5: AgenteRetencao.from_dict com dict SEM tipo_remarcacao → tipo_remarcacao = None
    - Test 6: AgenteRetencao.to_dict inclui rodada_negociacao, _slots_pool, tipo_remarcacao, id_agenda_original
    - Test 7: _detectar_tipo_remarcacao — mock consultar_agendamento_ativo retorna None → retorna "nova_consulta"
    - Test 8: _detectar_tipo_remarcacao — mock consultar_agendamento_ativo retorna agenda, verificar_lancamento_financeiro retorna False → retorna "nova_consulta"
    - Test 9: _detectar_tipo_remarcacao — mock consultar_agendamento_ativo retorna agenda, verificar_lancamento_financeiro retorna True → retorna "retorno"
  </behavior>
  <action>
    Modificar app/agents/retencao.py:

    1. Adicionar imports no topo (dentro de from __future__ import annotations):
       - from datetime import date
       - from app.agents.dietbox_worker import consultar_slots_disponiveis, consultar_agendamento_ativo, verificar_lancamento_financeiro

    2. Adicionar função module-level calcular_fim_janela(data_consulta: date) -> date:
       - Algoritmo (per D-05): encontra a sexta-feira da semana SEGUINTE à semana do agendamento
       - Semana começa na segunda. "Semana seguinte" = adicionar 7 dias, depois avançar para sexta
       - Ex: qualquer dia na semana 13-17/abr → semana seguinte começa em 20/abr → sexta é 24/abr
       - Implementação: dia_da_semana = data_consulta.weekday() (0=seg, 4=sex)
         dias_ate_prox_segunda = (7 - dia_da_semana) % 7 or 7  → se já for segunda, avança 7
         prox_segunda = data_consulta + timedelta(days=dias_ate_prox_segunda)
         → retorna prox_segunda + timedelta(days=4)  (segunda + 4 = sexta)

    3. Expandir __init__ do AgenteRetencao com novos campos:
       - self.tipo_remarcacao: str | None = None   # "retorno" | "nova_consulta"
       - self.id_agenda_original: str | None = None
       - self.fim_janela: date | None = None       # sexta da semana seguinte ao agendamento
       - self.rodada_negociacao: int = 0
       - self._slots_pool: list[dict] = []         # pool completo de slots (não apenas os 3 oferecidos)

    4. Atualizar to_dict() — adicionar os 5 novos campos:
       "tipo_remarcacao": self.tipo_remarcacao,
       "id_agenda_original": self.id_agenda_original,
       "fim_janela": self.fim_janela.isoformat() if self.fim_janela else None,
       "rodada_negociacao": self.rodada_negociacao,
       "_slots_pool": self._slots_pool,

    5. Atualizar from_dict() — usar .get(campo, default) para TODOS os novos campos:
       agent.tipo_remarcacao = data.get("tipo_remarcacao")
       agent.id_agenda_original = data.get("id_agenda_original")
       fim_janela_str = data.get("fim_janela")
       agent.fim_janela = date.fromisoformat(fim_janela_str) if fim_janela_str else None
       agent.rodada_negociacao = data.get("rodada_negociacao", 0)
       agent._slots_pool = data.get("_slots_pool", [])
       ATENÇÃO: campos existentes também devem usar .get() — verificar que _slots_oferecidos
       e _preferencia_horario também têm defaults (já existem via __init__, confirmar)

    6. Adicionar método privado _detectar_tipo_remarcacao(self) -> str:
       - Chama consultar_agendamento_ativo(id_paciente) — mas id_paciente pode não estar disponível
         no AgenteRetencao. Estratégia: buscar via telefone.
         Usar buscar_paciente_por_telefone(self.telefone) para obter id_paciente.
         Se não encontrar paciente → retorna "nova_consulta"
       - Se encontrar paciente, chama consultar_agendamento_ativo(id_paciente)
       - Se não houver agenda ativa → retorna "nova_consulta" (per D-04)
       - Se agenda ativa, chama verificar_lancamento_financeiro(agenda["id"])
       - Se lançamento financeiro existe → tipo = "retorno", salva self.id_agenda_original e self.fim_janela
       - Se não → tipo = "nova_consulta" (per D-03)
       - Salva self.tipo_remarcacao e retorna o tipo

    7. Modificar _fluxo_remarcacao — no início do fluxo (etapa "inicio"), ANTES de mostrar MSG_INICIO_REMARCACAO:
       - Chamar self._detectar_tipo_remarcacao()
       - Se tipo == "nova_consulta": mudar self.etapa para "redirecionando_atendimento"
         Retornar mensagem acolhedora: "Não localizei um agendamento já confirmado para você 😊
         Vou te passar para o fluxo de agendamento normal — me conta o que você está procurando!"
         (esta etapa sinaliza ao router que deve trocar para AgenteAtendimento)
       - Se tipo == "retorno": continuar com MSG_INICIO_REMARCACAO normal (per D-08)

    8. No trecho de busca de slots (etapa "coletando_preferencia"):
       - SUBSTITUIR a lógica atual de cálculo de janela
       - Novo cálculo (per D-05, D-06):
         data_inicio_busca = date.today() + timedelta(days=1)   # amanhã (per D-06)
         data_fim_janela = self.fim_janela or calcular_fim_janela(date.today())
         dias_a_frente = (data_fim_janela - date.today()).days
         Chamar consultar_slots_disponiveis(modalidade=self.modalidade, dias_a_frente=dias_a_frente, data_inicio=data_inicio_busca)
       - Salvar resultado completo em self._slots_pool (não apenas os 3 selecionados)

    Criar tests/test_retencao.py se não existir (arquivo novo). Usar unittest.mock.patch para mockar
    consultar_agendamento_ativo, verificar_lancamento_financeiro e buscar_paciente_por_telefone.
    Padrão de teste: mesmo estilo de test_router.py e test_dietbox_worker.py no projeto.
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_retencao.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - calcular_fim_janela exportada e testada (3 casos de data)
    - AgenteRetencao tem 5 novos campos em __init__, to_dict e from_dict
    - from_dict usa .get() para todos os campos novos — compatível com estados Phase 1 sem esses campos
    - _detectar_tipo_remarcacao testada com 3 cenários (sem paciente, paciente sem lançamento, paciente com lançamento)
    - python -m pytest tests/ -q não quebra nenhum teste existente
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retencao.py → Dietbox API | Dados de agendamento e financeiro vêm do Dietbox; a Ana confia neles para decidir prazo |
| Redis state → AgenteRetencao | Estado deserializado do Redis pode estar incompleto (Phase 1 não tinha os novos campos) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | from_dict() — campos novos ausentes em estado Phase 1 | mitigate | Usar .get(campo, default) em todos os novos campos; test_04 ao test_06 cobrem isso |
| T-02-01-02 | Denial of Service | consultar_agendamento_ativo — timeout Dietbox | mitigate | timeout=15 + try/except retorna None; fluxo cai para "nova_consulta" sem travar |
| T-02-01-03 | Information Disclosure | telefone do paciente em logs | mitigate | Logar apenas phone[-4:], nunca número completo (padrão já estabelecido no projeto) |
| T-02-01-04 | Spoofing | paciente afirma ter consulta quando não tem | accept | Verificação vem do Dietbox (fonte de verdade per D-25), não do paciente |
</threat_model>

<verification>
Após execução:

1. python -m pytest tests/test_dietbox_worker.py tests/test_retencao.py -q — todos verde
2. python -m pytest tests/ -q — nenhum teste existente quebrou
3. Verificar manualmente: from app.agents.retencao import calcular_fim_janela, AgenteRetencao
   - calcular_fim_janela(date(2026,4,14)) == date(2026,4,24)
   - AgenteRetencao.from_dict({"_tipo":"retencao","telefone":"test","nome":"Ana","etapa":"inicio"}) não lança KeyError
</verification>

<success_criteria>
- consultar_agendamento_ativo e verificar_lancamento_financeiro funcionam com mock HTTP
- calcular_fim_janela calcula corretamente "sexta da semana seguinte ao agendamento" para todos os dias da semana
- AgenteRetencao serializa/deserializa os 5 novos campos sem quebrar estados antigos (Phase 1)
- _detectar_tipo_remarcacao distingue os 3 casos: sem paciente, com paciente sem lançamento, com paciente com lançamento
- Zero regressões em testes existentes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-fluxo-de-remarca-o/02-01-SUMMARY.md`
</output>
