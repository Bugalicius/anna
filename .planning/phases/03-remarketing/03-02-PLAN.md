---
phase: 03-remarketing
plan: 02
type: execute
wave: 2
depends_on: ["03-01-PLAN.md"]
files_modified:
  - app/remarketing.py
  - app/agents/orchestrator.py
  - app/router.py
  - tests/test_remarketing.py
autonomous: true
requirements: [RMKT-01, RMKT-02, RMKT-03, RMKT-04, RMKT-06]
must_haves:
  truths:
    - "Sequencia de remarketing dispara em 24h, 7d e 30d apos silencio"
    - "MAX_REMARKETING = 3 e nao 5"
    - "Lead que recusa remarketing e movido para lead_perdido e nenhuma mensagem adicional e enviada"
    - "Remarketing nao dispara quando paciente tem conversa ativa no Redis"
    - "Orquestrador classifica recusou_remarketing como intencao valida"
  artifacts:
    - path: "app/remarketing.py"
      provides: "Sequencia corrigida 24h/7d/30d, MAX=3, check Redis"
      contains: "MAX_REMARKETING = 3"
    - path: "app/agents/orchestrator.py"
      provides: "Intencao recusou_remarketing"
      contains: "recusou_remarketing"
    - path: "app/router.py"
      provides: "Handler de recusou_remarketing"
      contains: "recusou_remarketing"
    - path: "tests/test_remarketing.py"
      provides: "Testes de logica de negocio do remarketing"
      contains: "test_remarketing_sequence"
  key_links:
    - from: "app/agents/orchestrator.py"
      to: "app/router.py"
      via: "intencao recusou_remarketing roteada para handler"
      pattern: "recusou_remarketing"
    - from: "app/router.py"
      to: "app/remarketing.py"
      via: "cancel_pending_remarketing ao detectar recusou_remarketing"
      pattern: "cancel_pending_remarketing"
    - from: "app/remarketing.py"
      to: "app/state_manager.py"
      via: "Verifica chave agent_state:{phone_hash} no Redis antes de disparar"
      pattern: "agent_state:"
---

<objective>
Corrigir a logica de negocio do remarketing: sequencia 24h/7d/30d, MAX=3, deteccao de lead perdido via nova intencao `recusou_remarketing`, e verificacao de conversa ativa no Redis antes de disparar.

Purpose: O codigo atual tem sequencia errada (5 mensagens, delays incorretos, templates comportamentais), MAX=5 (deveria ser 3), e nao detecta lead perdido nem verifica conversa ativa.

Output: Remarketing com regras corretas, orquestrador expandido, router tratando recusou_remarketing.
</objective>

<execution_context>
@C:/Users/Breno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Breno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remarketing/03-CONTEXT.md
@.planning/phases/03-remarketing/03-01-SUMMARY.md

<interfaces>
<!-- Apos Plan 03-01, estas interfaces estarao disponiveis -->

From app/remarketing.py (pos 03-01):
```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler

REMARKETING_SEQUENCE = [...]  # AINDA ERRADO — esta task corrige
MAX_REMARKETING = 5           # AINDA ERRADO — esta task corrige
RATE_LIMIT_PER_MIN = 30

async def _dispatch_due_messages(): ...   # async, usa await
def cancel_pending_remarketing(db, contact_id): ...  # sync, mantido
def can_schedule_remarketing(db, contact_id) -> bool: ...
def schedule_time_remarketing(db, contact_id, template, delay_hours, position): ...
def create_scheduler() -> AsyncIOScheduler: ...
```

From app/agents/orchestrator.py (ATUAL):
```python
IntencaoType = Literal["novo_lead", "tirar_duvida", "agendar", "pagar", "remarcar", "cancelar", "fora_de_contexto", "duvida_clinica"]
_INTENCOES_AGENTE1: set[str] = {"novo_lead", "tirar_duvida", "agendar", "pagar"}
_INTENCOES_AGENTE2: set[str] = {"remarcar", "cancelar"}
# _PROMPT_CLASSIFICACAO — lista opcoes de intencao
```

From app/router.py (ATUAL):
```python
_INTENCOES_INTERRUPT: frozenset[str] = frozenset({"remarcar", "cancelar", "duvida_clinica"})
_INTENCOES_INLINE: frozenset[str] = frozenset({"tirar_duvida", "fora_de_contexto"})
# route_message() roteia por agente_destino: "atendimento", "retencao", "escalacao", "padrao"
# cancel_pending_remarketing ja chamado quando stage in ("cold_lead", "remarketing_sequence", "remarketing")
```

From app/state_manager.py (NAO MODIFICAR):
```python
class RedisStateManager:
    KEY_PREFIX = "agent_state:"
    async def load(self, phone_hash) -> object | None  # retorna agente ou None
    # _client: aioredis.Redis
```

From app/tags.py (REFERENCIA — Tag.LEAD_PERDIDO ja existe):
```python
class Tag(str, Enum):
    LEAD_PERDIDO = "lead_perdido"
    REMARKETING = "remarketing"
# _TRANSICOES_VALIDAS: LEAD_PERDIDO aceita origem de NOVO_LEAD, REMARKETING, AGUARDANDO_PAGAMENTO
```

From app/models.py (REFERENCIA):
```python
class Contact(Base):
    phone_hash: Mapped[str]    # usado como chave Redis
    stage: Mapped[str]         # "new", "remarketing", "lead_perdido", etc.
    remarketing_count: Mapped[int]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Corrigir sequencia e MAX, adicionar recusou_remarketing ao orquestrador</name>
  <files>app/remarketing.py, app/agents/orchestrator.py, tests/test_remarketing.py</files>
  <behavior>
    - Test 1: REMARKETING_SEQUENCE tem exatamente 3 entries com delays 24, 168, 720 horas
    - Test 2: MAX_REMARKETING == 3
    - Test 3: BEHAVIORAL_TEMPLATES foi removido (nao existe mais)
    - Test 4: schedule_behavioral_remarketing foi removido ou raises NotImplementedError
    - Test 5: can_schedule_remarketing retorna False quando remarketing_count >= 3
    - Test 6: can_schedule_remarketing retorna False quando contact.stage == "lead_perdido"
    - Test 7: _classificar_intencao retorna "recusou_remarketing" para "nao tenho interesse"
    - Test 8: IntencaoType inclui "recusou_remarketing"
    - Test 9: rotear() com intencao recusou_remarketing retorna agente="remarketing_recusa"
  </behavior>
  <action>
    **Escrever testes primeiro** em `tests/test_remarketing.py`.

    **app/remarketing.py:**

    1. Substituir `REMARKETING_SEQUENCE` por (per D-01):
       ```python
       REMARKETING_SEQUENCE = [
           {"position": 1, "template": "ana_followup_24h", "delay_hours": 24},
           {"position": 2, "template": "ana_followup_7d", "delay_hours": 168},
           {"position": 3, "template": "ana_followup_30d", "delay_hours": 720},
       ]
       ```
    2. `MAX_REMARKETING = 3` (per D-01, D-04 — corrige de 5 para 3)
    3. Remover `BEHAVIORAL_TEMPLATES` dict completamente (deferido — segmentacao por comportamento)
    4. Remover `schedule_behavioral_remarketing()` completamente
    5. Em `can_schedule_remarketing()`: adicionar check `contact.stage == "lead_perdido"` retorna False
    6. Em `_dispatch_due_messages()`: ao atingir `MAX_REMARKETING`, mudar `contact.stage` para `"lead_perdido"` em vez de `"archived"` (per D-01 — apos 3 sem resposta, e lead perdido)

    **app/agents/orchestrator.py:**

    1. Adicionar `"recusou_remarketing"` ao `IntencaoType` Literal (per D-08)
    2. Adicionar ao `_PROMPT_CLASSIFICACAO` a opcao:
       ```
       - recusou_remarketing: lead informa que nao vai marcar consulta, nao tem interesse, pede para parar de enviar mensagens, diz "deixa pra la", "nao vou marcar", "pode tirar meu numero"
       ```
    3. Adicionar `"recusou_remarketing"` ao set de intencoes validas na validacao dentro de `_classificar_intencao()`
    4. Em `rotear()`: tratar `recusou_remarketing` retornando `{"agente": "remarketing_recusa", ...}` — novo tipo de agente_destino que o router tratara
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_remarketing.py -x -q</automated>
  </verify>
  <done>
    - REMARKETING_SEQUENCE tem 3 entries: 24h, 168h, 720h
    - MAX_REMARKETING = 3
    - BEHAVIORAL_TEMPLATES e schedule_behavioral_remarketing removidos
    - can_schedule_remarketing bloqueia lead_perdido
    - recusou_remarketing e intencao valida no orquestrador
    - Testes passam
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handler recusou_remarketing no router + farewell message</name>
  <files>app/router.py, tests/test_remarketing.py</files>
  <behavior>
    - Test 1: Quando intencao=recusou_remarketing, Ana envia mensagem de encerramento exata (D-09)
    - Test 2: Apos recusou_remarketing, contact.stage muda para "lead_perdido" via set_tag
    - Test 3: Apos recusou_remarketing, cancel_pending_remarketing e chamado
    - Test 4: Se _state_mgr tem estado Redis ativo, estado e deletado apos recusou_remarketing
    - Test 5: Mensagem de encerramento e exatamente: "Tudo bem! Posso perguntar o que pesou na decisao? So pra melhorar nosso atendimento :)"
  </behavior>
  <action>
    **Escrever testes primeiro** em `tests/test_remarketing.py` (adicionar ao mesmo arquivo).

    **app/router.py:**

    1. Importar `Tag, set_tag` de `app.tags` (ja importado)
    2. Antes do bloco `if agente_destino == "padrao":` (linha ~175), adicionar handler para `remarketing_recusa`:
       ```python
       # ── Recusa de remarketing — lead perdido (D-09, D-10) ────────────────
       if agente_destino == "remarketing_recusa":
           MSG_ENCERRAMENTO_REMARKETING = (
               "Tudo bem! Posso perguntar o que pesou na decisao? "
               "So pra melhorar nosso atendimento :)"
           )
           await _enviar(meta, phone, [MSG_ENCERRAMENTO_REMARKETING])

           # Cancela fila pendente e move para lead_perdido (D-10)
           with SessionLocal() as db:
               contact = db.query(Contact).filter_by(phone_hash=phone_hash).first()
               if contact:
                   cancel_pending_remarketing(db, contact.id)
                   set_tag(db, contact, Tag.LEAD_PERDIDO, force=True)
                   db.commit()

           # Deleta estado Redis se existir
           if _state_mgr:
               await _state_mgr.delete(phone_hash)
           return
       ```
    3. NOTA: A mensagem de encerramento usa o emoji unicode do D-09 no CONTEXT.md. Usar exatamente o texto de D-09.
    4. NAO modificar o bloco existente de `cancel_pending_remarketing` nas linhas 79-83 (per D-12 — comportamento mantido).
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_remarketing.py -x -q</automated>
  </verify>
  <done>
    - recusou_remarketing no router envia mensagem de encerramento exata
    - Contact.stage muda para lead_perdido
    - Fila de remarketing cancelada
    - Estado Redis deletado
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verificacao de conversa ativa no Redis antes de disparar</name>
  <files>app/remarketing.py, tests/test_remarketing.py</files>
  <behavior>
    - Test 1: Quando existe chave agent_state:{phone_hash} no Redis, entry NAO e disparada (skip)
    - Test 2: Entry pulada por conversa ativa NAO e cancelada nem reagendada — permanece pending
    - Test 3: Quando NAO existe chave agent_state:{phone_hash}, entry e disparada normalmente
    - Test 4: Entry pulada por conversa ativa e tentada novamente no proximo ciclo (status continua pending)
  </behavior>
  <action>
    **Escrever testes primeiro** em `tests/test_remarketing.py` (adicionar ao mesmo arquivo).

    **app/remarketing.py** — Em `_dispatch_due_messages()` (per D-11):

    1. Antes do bloco de envio (apos carregar `contact`), verificar se existe estado Redis ativo:
       ```python
       # D-11: pular se paciente tem conversa ativa no Redis
       state_key = f"agent_state:{contact.phone_hash}"
       has_active = await redis_client.exists(state_key)
       if has_active:
           logger.info("Remarketing skip — conversa ativa para %s", contact.phone_hash[-4:])
           continue  # pula este ciclo, tenta no proximo (1 min)
       ```
    2. O `redis_client` ja e `aioredis.Redis` (feito no Plan 03-01). Usar `await redis_client.exists(state_key)`.
    3. NAO cancelar nem reagendar — simplesmente `continue`. A entry permanece pending e sera retestada no proximo ciclo do scheduler (1 minuto).
    4. Posicionar este check APOS os checks de `stage == "archived"/"lead_perdido"` e `phone_e164`, mas ANTES do `try: meta.send_template()`.
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_remarketing.py -x -q</automated>
  </verify>
  <done>
    - Remarketing nao dispara quando ha conversa ativa no Redis
    - Entry permanece pending (nao cancelada)
    - Proximo ciclo tenta novamente
    - Suite completa passa
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user message -> orchestrator | Input nao confiavel classificado por LLM |
| scheduler -> Redis state check | Leitura de estado Redis para verificar conversa ativa |
| router -> database | Alteracao de stage para lead_perdido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Spoofing | recusou_remarketing | mitigate | Classificacao via LLM + validacao contra set de intencoes validas no orchestrator |
| T-03-05 | Tampering | cancel_pending_remarketing | accept | Funcao interna, nao exposta via API — risco baixo |
| T-03-06 | Denial of Service | _dispatch_due_messages Redis check | mitigate | exists() e O(1) no Redis, sem impacto de performance |
| T-03-07 | Information Disclosure | MSG_ENCERRAMENTO | accept | Mensagem generica sem dados sensiveis |
</threat_model>

<verification>
1. `python -m pytest tests/test_remarketing.py -x -q` — todos passam
2. `python -m pytest tests/ -q` — suite completa sem regressoes
3. `python -c "from app.remarketing import MAX_REMARKETING, REMARKETING_SEQUENCE; assert MAX_REMARKETING == 3; assert len(REMARKETING_SEQUENCE) == 3"` — confirma constantes
4. `grep -r "BEHAVIORAL_TEMPLATES" app/` — nenhum resultado (removido)
5. `grep "recusou_remarketing" app/agents/orchestrator.py` — presente
</verification>

<success_criteria>
- Sequencia fixa 24h/7d/30d com MAX=3
- recusou_remarketing classificado e tratado corretamente
- Lead perdido cancela fila e para de receber mensagens
- Conversa ativa bloqueia disparo temporariamente
- Suite completa de testes passa
</success_criteria>

<output>
After completion, create `.planning/phases/03-remarketing/03-02-SUMMARY.md`
</output>
