---
phase: 03-remarketing
plan: 03
type: execute
wave: 3
depends_on: ["03-02-PLAN.md"]
files_modified:
  - app/remarketing.py
  - tests/test_remarketing_templates.py
autonomous: true
requirements: [RMKT-01, RMKT-02, RMKT-03, RMKT-05]
must_haves:
  truths:
    - "Mensagem 24h usa send_text com texto exato de D-02"
    - "Mensagens 7d e 30d usam send_template quando aprovados"
    - "Se template Meta nao aprovado, mensagem fica na fila sem erro"
    - "Textos das mensagens sao exatamente os definidos pelo usuario (D-02, D-03, D-04)"
    - "Nomes dos templates Meta sao ana_followup_24h, ana_followup_7d, ana_followup_30d"
  artifacts:
    - path: "app/remarketing.py"
      provides: "Constantes MSG_FOLLOWUP_*, _enviar_remarketing(), logica de envio"
      contains: "MSG_FOLLOWUP_24H"
    - path: "tests/test_remarketing_templates.py"
      provides: "Testes de templates e logica de envio"
      contains: "test_enviar_remarketing"
  key_links:
    - from: "app/remarketing.py"
      to: "app/meta_api.py"
      via: "_enviar_remarketing usa send_text para 24h e send_template para 7d/30d"
      pattern: "send_text|send_template"
    - from: "app/remarketing.py"
      to: "app/remarketing.py"
      via: "MSG_FOLLOWUP_* constants usadas em _enviar_remarketing"
      pattern: "MSG_FOLLOWUP_"
---

<objective>
Implementar os textos exatos das mensagens de remarketing (aprovados pelo usuario) e a logica de envio: send_text para 24h, send_template para 7d/30d com graceful degradation quando templates Meta nao estao aprovados.

Purpose: Os textos foram escritos pela Ana e aprovados pelo Breno — sao a fonte de verdade. O codigo precisa enviar exatamente esses textos, usando o canal correto (texto direto vs template Meta conforme janela de 24h da Meta).

Output: Constantes de mensagem, funcao `_enviar_remarketing()`, guia de submissao de templates.
</objective>

<execution_context>
@C:/Users/Breno/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Breno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-remarketing/03-CONTEXT.md
@.planning/phases/03-remarketing/03-02-SUMMARY.md

<interfaces>
<!-- Apos Plan 03-02, estas interfaces estarao disponiveis -->

From app/remarketing.py (pos 03-02):
```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler
import redis.asyncio as aioredis

REMARKETING_SEQUENCE = [
    {"position": 1, "template": "ana_followup_24h", "delay_hours": 24},
    {"position": 2, "template": "ana_followup_7d", "delay_hours": 168},
    {"position": 3, "template": "ana_followup_30d", "delay_hours": 720},
]
MAX_REMARKETING = 3
RATE_LIMIT_PER_MIN = 30

async def _dispatch_due_messages(): ...  # query due entries, check Redis, send
def cancel_pending_remarketing(db, contact_id): ...
def can_schedule_remarketing(db, contact_id) -> bool: ...
def schedule_time_remarketing(db, contact_id, template, delay_hours, position): ...
def create_scheduler() -> AsyncIOScheduler: ...
```

From app/meta_api.py (NAO MODIFICAR):
```python
class MetaAPIClient:
    async def send_text(self, to: str, text: str) -> dict
    async def send_template(self, to: str, template_name: str, language: str = "pt_BR", components: list | None = None) -> dict
```

From app/models.py (REFERENCIA):
```python
class RemarketingQueue(Base):
    template_name: Mapped[str]      # "ana_followup_24h", "ana_followup_7d", "ana_followup_30d"
    sequence_position: Mapped[int]  # 1, 2, 3
    status: Mapped[str]             # pending | sent | cancelled | failed
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constantes MSG_FOLLOWUP_* e nomes de templates</name>
  <files>app/remarketing.py, tests/test_remarketing_templates.py</files>
  <behavior>
    - Test 1: MSG_FOLLOWUP_24H contem "Eiii!" e "aí" e "dúvida" e "à vontade" e "tô" e "é só"
    - Test 2: MSG_FOLLOWUP_7D contem "Passando pra saber" e "você" e "Às vezes" e "relação"
    - Test 3: MSG_FOLLOWUP_30D contem "última passagem por aqui" e "você" e "adiar"
    - Test 4: TEMPLATE_NAMES dict mapeia position 1->ana_followup_24h, 2->ana_followup_7d, 3->ana_followup_30d
    - Test 5: MSG_FOLLOWUP_* nao contem o numero 31 99205-9211
  </behavior>
  <action>
    **Escrever testes primeiro** em `tests/test_remarketing_templates.py`.

    **app/remarketing.py** — Adicionar constantes de mensagem EXATAMENTE como em D-02, D-03, D-04:

    ```python
    # ── Textos aprovados — fonte de verdade (D-02, D-03, D-04) ──────────────

    MSG_FOLLOWUP_24H = (
        "Eiii! \U0001f60a Tudo bem por aí?\n\n"
        "Fico pensando se ficou alguma dúvida sobre a consulta com a Thaynara... "
        "Pode me perguntar à vontade, tô aqui pra isso! \U0001f49a\n\n"
        "Quando quiser marcar é só me falar \U0001f4c5"
    )

    MSG_FOLLOWUP_7D = (
        "Oii! Passando pra saber se você teve chance de pensar na consulta "
        "com a Thaynara \U0001f33f\n\n"
        "Às vezes bate aquela dúvida se vale a pena... mas a maioria das pacientes "
        "conta que a primeira consulta já muda bastante a relação com a alimentação \U0001f60a\n\n"
        "Se quiser conversar sobre qualquer coisa antes de decidir, me chama! \U0001f449"
    )

    MSG_FOLLOWUP_30D = (
        "Eiii, última passagem por aqui! \U0001f49a\n\n"
        "Sei que a vida corrida faz a gente adiar algumas coisas... "
        "Se um dia você quiser dar esse passo, pode me chamar que a gente vê "
        "o melhor horário pra você com a Thaynara \U0001f4c5\n\n"
        "Qualquer coisa, estarei por aqui! \U0001f60a"
    )
    ```

    **CRITICO:** Copiar os textos EXATAMENTE como aparecem no 03-CONTEXT.md D-02, D-03, D-04. Incluir emojis unicode corretos. Manter quebras de linha identicas.

    Adicionar dict de mapeamento:
    ```python
    TEMPLATE_NAMES = {
        1: "ana_followup_24h",
        2: "ana_followup_7d",
        3: "ana_followup_30d",
    }

    # Textos por posicao na sequencia (usados por _enviar_remarketing)
    _MSG_POR_POSICAO = {
        1: MSG_FOLLOWUP_24H,
        2: MSG_FOLLOWUP_7D,
        3: MSG_FOLLOWUP_30D,
    }
    ```
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_remarketing_templates.py -x -q</automated>
  </verify>
  <done>
    - Constantes MSG_FOLLOWUP_* existem com textos exatos
    - TEMPLATE_NAMES mapeia posicoes para nomes corretos
    - Numero interno ausente das mensagens
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Funcao _enviar_remarketing com logica send_text/send_template</name>
  <files>app/remarketing.py, tests/test_remarketing_templates.py</files>
  <behavior>
    - Test 1: position=1 (24h) chama send_text com MSG_FOLLOWUP_24H
    - Test 2: position=1 (24h), send_text falha com erro 131026 (janela fechada) -> loga warning, retorna False
    - Test 3: position=2 (7d) com TEMPLATES_APPROVED=True chama send_template("ana_followup_7d")
    - Test 4: position=2 (7d) com TEMPLATES_APPROVED=False -> retorna False sem enviar, entry permanece pending
    - Test 5: position=3 (30d) com TEMPLATES_APPROVED=True chama send_template("ana_followup_30d")
    - Test 6: _dispatch_due_messages usa _enviar_remarketing em vez de send_template direto
    - Test 7: Entry com send bem-sucedido marca status="sent", incrementa remarketing_count
    - Test 8: Entry com send falhando marca status="failed"
  </behavior>
  <action>
    **Escrever testes primeiro** em `tests/test_remarketing_templates.py` (adicionar ao mesmo arquivo).

    **app/remarketing.py:**

    1. Adicionar flag de controle (per D-06):
       ```python
       # Templates Meta aprovados? Mudar para True apos aprovacao no Business Manager (D-06)
       # Env var permite ativar sem redeploy: REMARKETING_TEMPLATES_APPROVED=true
       TEMPLATES_APPROVED = os.environ.get("REMARKETING_TEMPLATES_APPROVED", "false").lower() == "true"
       ```

    2. Criar `async def _enviar_remarketing(meta, to, entry) -> bool` (per D-05, D-06):
       ```python
       async def _enviar_remarketing(
           meta: "MetaAPIClient", to: str, entry: "RemarketingQueue"
       ) -> bool:
           """
           Envia mensagem de remarketing usando o canal correto.

           - Position 1 (24h): tenta send_text primeiro (D-05).
             Se Meta rejeitar por janela fechada (erro 131026), loga e retorna False.
           - Position 2/3 (7d/30d): usa send_template se aprovados (D-06).
             Se templates nao aprovados, retorna False (entry permanece pending).

           Returns: True se enviado com sucesso, False se falhou ou nao disponivel.
           """
           position = entry.sequence_position

           if position == 1:
               # 24h — tenta texto livre (D-05)
               texto = _MSG_POR_POSICAO.get(position)
               if not texto:
                   return False
               try:
                   await meta.send_text(to=to, text=texto)
                   return True
               except Exception as e:
                   error_str = str(e)
                   if "131026" in error_str or "re-engage" in error_str.lower():
                       logger.warning("Janela 24h fechada para %s — aguardando template", to[-4:])
                   else:
                       logger.error("Falha send_text remarketing para %s: %s", to[-4:], e)
                   return False
           else:
               # 7d/30d — usa template Meta (D-06)
               if not TEMPLATES_APPROVED:
                   logger.info("Templates Meta nao aprovados — skip position %d para %s", position, to[-4:])
                   return False
               template_name = TEMPLATE_NAMES.get(position)
               if not template_name:
                   return False
               try:
                   await meta.send_template(to=to, template_name=template_name)
                   return True
               except Exception as e:
                   logger.error("Falha send_template %s para %s: %s", template_name, to[-4:], e)
                   return False
       ```

    3. Em `_dispatch_due_messages()`: substituir o bloco `try: meta.send_template(...)` por:
       ```python
       success = await _enviar_remarketing(meta, contact.phone_e164, entry)
       if success:
           entry.status = "sent"
           entry.sent_at = now
           if entry.counts_toward_limit:
               contact.remarketing_count += 1
           if contact.remarketing_count >= MAX_REMARKETING:
               contact.stage = "lead_perdido"
           db.commit()
           await asyncio.sleep(2)  # intervalo minimo entre disparos
       else:
           # Para position 1 (janela fechada): marca failed
           # Para position 2/3 (templates nao aprovados): mantém pending
           if entry.sequence_position == 1:
               entry.status = "failed"
               db.commit()
           # positions 2/3 sem template aprovado: nao muda status — retry no proximo ciclo
       ```

    4. Adicionar comentario-guia sobre submissao de templates (per D-07):
       ```python
       # ── Guia: Submissao de Templates no Meta Business Manager ────────────
       #
       # Templates necessarios (D-07):
       #   1. ana_followup_24h  — categoria: MARKETING
       #   2. ana_followup_7d   — categoria: MARKETING
       #   3. ana_followup_30d  — categoria: MARKETING
       #
       # Passos:
       #   1. Acesse business.facebook.com > WhatsApp Manager > Message Templates
       #   2. Crie cada template com categoria "Marketing"
       #   3. Idioma: Portuguese (BR) — codigo pt_BR
       #   4. Corpo: copie EXATAMENTE o texto de MSG_FOLLOWUP_* (incluindo emojis)
       #   5. Submeta para revisao — aprovacao pode levar ate 48h
       #   6. Apos aprovacao, defina REMARKETING_TEMPLATES_APPROVED=true no .env
       #
       # Enquanto templates nao aprovados:
       #   - Mensagem 24h: tenta send_text (funciona se paciente escreveu nas ultimas 24h)
       #   - Mensagens 7d/30d: ficam na fila, nao sao enviadas (sem erro)
       # ────────────────────────────────────────────────────────────────────────
       ```
  </action>
  <verify>
    <automated>cd /c/Users/Breno/Desktop/agente && python -m pytest tests/test_remarketing_templates.py -x -q</automated>
  </verify>
  <done>
    - _enviar_remarketing envia por canal correto conforme position
    - send_text para 24h com fallback graceful para janela fechada
    - send_template para 7d/30d apenas quando TEMPLATES_APPROVED=true
    - _dispatch_due_messages usa _enviar_remarketing
    - Guia de submissao de templates documentado no codigo
    - Suite completa passa: `python -m pytest tests/ -q`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> Meta Cloud API | Envio de mensagens para numeros reais |
| env var -> app | REMARKETING_TEMPLATES_APPROVED controlado por .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | TEMPLATES_APPROVED env var | accept | Controlado pelo dono do servidor — nao e input externo |
| T-03-09 | Information Disclosure | MSG_FOLLOWUP_* | mitigate | Textos nao contem dados sensiveis, numero interno ausente |
| T-03-10 | Denial of Service | send_text fallback | mitigate | Erro 131026 capturado e logado, nao causa retry infinito (entry vai para failed) |
</threat_model>

<verification>
1. `python -m pytest tests/test_remarketing_templates.py -x -q` — todos passam
2. `python -m pytest tests/ -q` — suite completa sem regressoes
3. `grep "99205" app/remarketing.py` — nenhum resultado (numero interno ausente)
4. `python -c "from app.remarketing import MSG_FOLLOWUP_24H; assert 'Eiii' in MSG_FOLLOWUP_24H"` — confirma texto
5. `python -c "from app.remarketing import TEMPLATES_APPROVED; print(TEMPLATES_APPROVED)"` — False por padrao
</verification>

<success_criteria>
- Textos de mensagem sao EXATAMENTE os aprovados pelo usuario (D-02, D-03, D-04)
- Canal correto usado por posicao: send_text (24h), send_template (7d/30d)
- Graceful degradation: sem template aprovado, 7d/30d ficam na fila sem erro
- Erro de janela fechada (131026) tratado sem crash
- Guia de submissao de templates no codigo
- Suite completa de testes passa
</success_criteria>

<output>
After completion, create `.planning/phases/03-remarketing/03-03-SUMMARY.md`
</output>
